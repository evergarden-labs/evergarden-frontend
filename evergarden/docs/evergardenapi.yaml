openapi: 3.1.0

info:
  title: EverGarden API
  version: 0.1.0-draft
  summary: 여행 아카이빙 플랫폼 에버가든의 백엔드 API

  description: |
    # 이 문서의 지위

    **이 파일은 에버가든 API의 유일한 정본(single source of truth)입니다.**
    클라이언트와 서버는 이 문서에 적힌 계약만을 근거로 구현하며,
    문서와 구현이 다르면 **구현을 고칩니다.**
    계약을 바꿔야 할 때는 코드를 먼저 고치지 말고 이 파일을 먼저 고칩니다.

    이 문서는 아래 네 문서를 근거로 작성했습니다. 근거가 없는 엔드포인트는 만들지 않습니다.

    | 근거 문서 | 위치 |
    |---|---|
    | 유저 스토리 카탈로그 | `docs/user-stories.tsv` (103행) |
    | ERD | 테이블 25개 |
    | 오류 코드 표 | `docs/error-codes.md` |
    | 결정 기록(ADR) | `docs/decisions.md` |
    | 실시간 편집 메시지 | `docs/realtime-editing.md` (WebSocket, OpenAPI 밖) |

    모든 오퍼레이션에는 근거가 된 스토리 ID를 `x-user-stories`로 붙였습니다.

    ---

    # 1. 응답 봉투

    ## 성공

    모든 성공 응답은 `data`와 `meta` 두 필드를 가진 객체입니다.

    ```json
    { "data": { "...": "..." }, "meta": null }
    ```

    - `data` — 실제 응답 본문. 반환할 것이 없으면 `null`
    - `meta` — 목록의 페이지 정보 등 부가 정보. 없으면 `null`

    **본문이 없는 응답에도 `204`를 쓰지 않고 `200`에 `data: null`을 담습니다.**
    클라이언트가 응답 처리 분기를 하나로 유지할 수 있게 하기 위해서입니다.

    ## 실패

    모든 실패 응답은 `error` 하나만 가집니다. 성공 봉투와 필드가 겹치지 않습니다.

    ```json
    { "error": { "code": "TRIP_NOT_FOUND", "message": "여행 일정을 찾을 수 없습니다.", "details": null } }
    ```

    - `code` — `docs/error-codes.md`에 정의된 코드만 사용합니다. **여기 없는 코드를 새로 만들지 않습니다.**
    - `message` — 사용자에게 그대로 보여줄 수 있는 한국어 문장
    - `details` — 코드별 부가 정보. 없으면 `null`

    ---

    # 2. 인증

    JWT Bearer 토큰을 `Authorization` 헤더에 담습니다.

    ```
    Authorization: Bearer <accessToken>
    ```

    - 액세스 토큰 만료 30분, 리프레시 토큰 만료 7일
    - 소셜 로그인은 **앱 SDK 방식**입니다. 앱이 Google·Kakao·Naver SDK로 받은 토큰을
      `POST /auth/social/{provider}`에 넘기면 서버가 검증하고 자체 JWT를 발급합니다.
      서버가 소셜 로그인 페이지로 리다이렉트하지 않습니다.
    - 리프레시 토큰은 **요청 본문**으로 주고받습니다. 쿠키를 쓰지 않습니다.
    - `security`를 전역으로 걸어두었고, 인증이 필요 없는 오퍼레이션에만 `security: []`로 해제합니다.

    > **관리자 인증** — 관리자도 같은 Bearer 스킴을 쓰고 토큰의 역할 클레임으로 구분합니다(ADR-036).
    > `securitySchemes`는 하나뿐입니다. 관리자 계정 자체는 소셜이 아닌 별도 저장소(`admins`)를 씁니다.

    ---

    # 3. 생략 규칙 — 오퍼레이션마다 적지 않는 응답

    아래 응답은 **전역 규칙**이며 각 오퍼레이션에 반복해 적지 않습니다.
    적혀 있지 않다고 해서 발생하지 않는다는 뜻이 아닙니다.

    | 응답 | 적용 범위 | 사용 코드 |
    |---|---|---|
    | `401` | 인증이 필요한 모든 오퍼레이션 | `UNAUTHENTICATED` · `TOKEN_EXPIRED` · `TOKEN_INVALID` |
    | `403` | 인증이 필요한 모든 오퍼레이션 | `USER_BLOCKED` · `USER_WITHDRAWN` |
    | `500` | 모든 오퍼레이션 | `INTERNAL_ERROR` |

    각 오퍼레이션에는 **그 오퍼레이션에서만 발생하는** `400` · `403` · `404` · `409` · `503`만 적습니다.
    `security: []`로 인증을 해제한 오퍼레이션은 위 `401` · `403` 규칙에서 제외됩니다.

    ---

    # 4. 목록 나누기 (페이지네이션)

    두 방식을 쓰며, 화면 성격에 따라 나눴습니다.

    ## 커서 방식 — 무한 스크롤 화면

    아카이브 목록, 타임캡슐 목록, 커뮤니티 피드에 사용합니다.

    - 요청: `cursor`(첫 페이지는 생략), `size`
    - `meta`: `{ "nextCursor": "...", "hasNext": true }`
    - `nextCursor`는 서버가 만든 불투명한 문자열입니다. 클라이언트가 해석하지 않습니다.

    ## 번호 방식 — 그 외 목록

    일정 목록, 좋아요 목록, 알림 목록, 관리자 화면에 사용합니다.

    - 요청: `page`(1부터 시작), `size`
    - `meta`: `{ "page": 1, "size": 20, "totalElements": 137, "totalPages": 7 }`

    두 방식 모두 `size` 기본값 **20**, 최대 **50**입니다.

    ---

    # 5. 확장 필드

    | 필드 | 뜻 |
    |---|---|
    | `x-user-stories` | 이 오퍼레이션의 근거가 된 유저 스토리 ID |
    | `x-implementation-status` | `not-implemented`이면 계약만 있고 구현 계획이 확정되지 않은 상태 |

    본문 안의 `[가정]` 태그는 **근거 문서에 없어 판단이 필요한 값**을 표시합니다.
    추측해서 채우지 않았으며, 무엇이 미정이고 누가 정해야 하는지 함께 적었습니다.

    ---

    # 6. 작성 진행 상황

    도메인 단위로 나누어 작성합니다.

    | 도메인 | 상태 |
    |---|---|
    | 공통 규약 · 인증 · 온보딩 · 마이페이지 | **작성 완료** |
    | 플래너 | **작성 완료** |
    | 지도 · 정원 | **작성 완료** |
    | 타임캡슐 | **작성 완료** |
    | 미디어 | **작성 완료** |
    | 아카이브 | **작성 완료** |
    | 커뮤니티 | **작성 완료** |
    | 알림 | **작성 완료** |
    | 관리자 | **작성 완료** |

servers:
  - url: https://api.evergarden.example/api/v1
    description: 운영
  - url: http://localhost:8080/api/v1
    description: 로컬 개발

security:
  - bearerAuth: []

tags:
  - name: 인증
    description: 소셜 로그인, 토큰 재발급, 로그아웃, 회원 탈퇴
  - name: 온보딩
    description: 가입 직후 초기 설정
  - name: 마이페이지
    description: 내 프로필 조회와 수정
  - name: 플래너
    description: 여행 일정(코스) 생성과 편집
  - name: 장소
    description: 한국관광 콘텐츠랩 기반 관광지 검색과 조회
  - name: 지도
    description: 지역 방문 인증과 지역별 관광 정보
  - name: 정원
    description: 방문 보상으로 모은 식물과 오브젝트
  - name: 미디어
    description: 사진·영상 업로드
  - name: 타임캡슐
    description: 조건이 충족되면 열리는 봉인 기록
  - name: 아카이브
    description: 테마 앨범 생성과 편집
  - name: 공동 편집
    description: 아카이브를 여럿이 함께 만들기
  - name: 커뮤니티
    description: 코스·아카이브 공유와 피드
  - name: 댓글
    description: 게시물의 댓글과 대댓글
  - name: 신고
    description: 부적절한 게시물·댓글 신고
  - name: 알림
    description: 받은 알림과 수신 설정
  - name: 관리자
    description: 운영자 전용 기능

paths:

  # ------------------------------------------------------------------
  # 인증
  # ------------------------------------------------------------------

  /auth/social/{provider}:
    post:
      tags: [인증]
      operationId: loginWithSocial
      summary: 소셜 로그인 · 회원가입
      x-user-stories: [AUTH-01, AUTH-02, AUTH-06]
      security: []
      description: |
        앱이 소셜 SDK로 받은 액세스 토큰을 서버가 검증하고 자체 JWT를 발급합니다.
        가입한 적이 없는 소셜 계정이면 회원가입이 함께 일어나며(AUTH-01),
        응답의 `isNewUser`로 구분합니다.

        온보딩을 마치지 않은 사용자는 `onboardingCompleted`가 `false`이며,
        앱은 초기 설정 화면으로 보냅니다.

        **차단된 계정의 로그인 시도(AUTH-06)도 이 오퍼레이션에서 처리됩니다.**
        `403 USER_BLOCKED`의 `details`에 사유와 차단 시점이 담겨,
        앱이 로그인 화면에서 이유를 보여줄 수 있습니다.
        차단된 사용자는 앱에 들어올 수 없어 알림 목록으로는 안내할 수 없습니다.
      parameters:
        - $ref: '#/components/parameters/Provider'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [socialAccessToken]
              properties:
                socialAccessToken:
                  type: string
                  description: 소셜 SDK가 발급한 액세스 토큰
      responses:
        '200':
          description: 로그인 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/AuthResult' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — `socialAccessToken`이 비어 있음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '401':
          description: |
            `SOCIAL_AUTH_FAILED` — 소셜 제공자가 토큰을 거부함
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `USER_BLOCKED` — 차단된 회원. `details.reason`·`details.blockedAt`으로 사유 안내(AUTH-06)
            `USER_WITHDRAWN` — 탈퇴한 회원. 유예 중이면 `details.restorableUntil`이 담기고,
            앱은 `POST /auth/restore`로 복구를 권합니다(ADR-054)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `SOCIAL_ACCOUNT_ALREADY_LINKED` — 이미 다른 계정에 연결된 소셜 계정
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /auth/restore:
    post:
      tags: [인증]
      operationId: restoreAccount
      summary: 탈퇴한 계정 복구
      x-user-stories: [AUTH-07]
      security: []
      description: |
        30일 유예 안에 있는 계정을 되살립니다(ADR-054).
        탈퇴할 때 쓰던 소셜 계정의 토큰으로 본인을 확인합니다 —
        로그인이 막혀 있어 액세스 토큰을 쓸 수 없습니다.

        복구하면 `users.status`가 `ACTIVE`로 돌아가고 닉네임·프로필이 그대로 살아납니다.
        게시물과 댓글의 작성자 표시도 원래 닉네임으로 돌아옵니다.

        **되돌아오지 않는 것이 하나 있습니다.** 탈퇴할 때 종료된 공동 편집은
        복구해도 다시 열리지 않습니다 — 그 사이 참여자들이 이미 복제해 갔을 수 있어서
        되돌리면 오히려 상태가 엉킵니다.

        유예가 지났으면 `RESTORE_PERIOD_EXPIRED`입니다. 새로 가입해야 합니다.
      parameters:
        - $ref: '#/components/parameters/Provider'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [socialAccessToken]
              properties:
                socialAccessToken:
                  type: string
                  description: 탈퇴할 때 연결돼 있던 소셜 계정의 액세스 토큰
      responses:
        '200':
          description: 복구 완료. 바로 로그인 상태가 됩니다
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/AuthResult' }
                  meta: { type: 'null' }
        '400':
          description: |
            `RESTORE_PERIOD_EXPIRED` — 30일 유예가 지났습니다. 새로 가입해야 합니다
            `INVALID_REQUEST` — 탈퇴 상태가 아닌 계정
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '401':
          description: |
            `SOCIAL_AUTH_FAILED` — 소셜 제공자가 토큰을 거부함
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `USER_NOT_FOUND` — 그 소셜 계정으로 탈퇴한 기록이 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /auth/token/refresh:
    post:
      tags: [인증]
      operationId: refreshToken
      summary: 액세스 토큰 재발급
      x-user-stories: [AUTH-05]
      security: []
      description: |
        액세스 토큰이 만료되어 `TOKEN_EXPIRED`를 받으면 이 오퍼레이션으로 재발급합니다.
        재발급에 성공하면 앱은 중단됐던 요청을 재시도하고 이전 작업 문맥으로 돌아갑니다.

        리프레시 토큰도 함께 새로 발급합니다(rotation). 이전 리프레시 토큰은 즉시 폐기됩니다.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refreshToken]
              properties:
                refreshToken: { type: string }
      responses:
        '200':
          description: 재발급 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TokenPair' }
                  meta: { type: 'null' }
        '401':
          description: |
            `REFRESH_TOKEN_EXPIRED` — 만료되었거나 폐기된 리프레시 토큰. 재로그인이 필요합니다
            `TOKEN_INVALID` — 서명 불일치 또는 변조
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /auth/logout:
    post:
      tags: [인증]
      operationId: logout
      summary: 로그아웃
      x-user-stories: [AUTH-03]
      description: |
        현재 사용자의 **모든** 리프레시 토큰을 폐기합니다.
        기기별로 선택 폐기하지 않는 이유는, 유저 스토리(AUTH-03)가
        "현재 로그인 세션이 종료된다"로만 되어 있고 다중 기기 관리 스토리가 없기 때문입니다.
      responses:
        '200':
          description: 로그아웃 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }

  # ------------------------------------------------------------------
  # 마이페이지 · 온보딩
  # ------------------------------------------------------------------

  /users/me:
    get:
      tags: [마이페이지]
      operationId: getMyProfile
      summary: 내 프로필 조회
      x-user-stories: [MY-01]
      description: |
        닉네임, 프로필 이미지와 내 기록 현황을 반환합니다.

        기록 현황은 네 갈래로 보여줍니다(ADR-034) — 기록, 정원, 커뮤니티, 타임캡슐.
        숫자가 아홉 개라 화면에서는 갈래별로 묶어서 배치하는 편이 읽힙니다.

        정원과 타임캡슐 숫자는 각자 전용 화면(GARDEN-01, TC-02)에도 있습니다.
        여기서는 **요약해서 보여주고 해당 화면으로 보내는 용도**입니다.
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/MyProfile' }
                  meta: { type: 'null' }

    patch:
      tags: [마이페이지]
      operationId: updateMyProfile
      summary: 프로필 수정
      x-user-stories: [MY-02]
      description: |
        닉네임과 프로필 이미지를 변경합니다. 보내지 않은 필드는 변경하지 않습니다.

        **온보딩 완료 처리는 하지 않습니다.** 가입 직후 초기 설정은
        `PATCH /users/me/onboarding`을 씁니다(ADR-021).
        온보딩을 마치지 않은 사용자가 이 오퍼레이션을 호출해도 프로필만 바뀝니다.

        **닉네임은 중복될 수 없습니다**(ADR-025). 이미 쓰이고 있으면 `409`입니다.

        **한글·영문·숫자만 쓸 수 있습니다**(ADR-053). 공백과 특수문자는 거절합니다.
        금칙어 필터는 두지 않고 신고(COMM-10)로 대응합니다.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProfileUpdateRequest' }
      responses:
        '200':
          description: 저장 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/MyProfile' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 닉네임 길이 위반 등 검증 실패
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `NICKNAME_DUPLICATED` — 이미 쓰이고 있는 닉네임(ADR-025)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [인증]
      operationId: withdrawMe
      summary: 회원 탈퇴
      x-user-stories: [AUTH-04]
      description: |
        계정을 탈퇴 상태로 전환하고 모든 리프레시 토큰을 폐기합니다.
        행을 삭제하지 않고 `users.status`를 `WITHDRAWN`으로 바꿉니다(ERD 설계 노트 6).

        > **[가정] 탈퇴 후 데이터 처리 정책이 정해지지 않았습니다.**
        > 유저 스토리 AUTH-04의 결과가 "정책에 따라 데이터가 처리된다"인데
        > 그 정책이 어느 문서에도 없습니다. 특히 아래 세 가지가 미정입니다.
        >
        > - 작성한 게시물과 댓글을 남길지 지울지
        > - 공동 편집 중이던 아카이브에서 어떻게 빠질지
        > - 유예 기간을 두고 복구를 허용할지
        >
        > 이 문서는 **계정 상태 전환과 토큰 폐기까지만** 계약으로 확정했습니다.
        > 결정 주체: 기획 + 법무.
      responses:
        '200':
          description: 탈퇴 처리 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }

  /users/me/onboarding:
    patch:
      tags: [온보딩]
      operationId: completeOnboarding
      summary: 초기 설정
      x-user-stories: [ONB-01]
      description: |
        가입 직후 닉네임과 프로필 이미지를 입력하고 온보딩을 완료 처리합니다.
        성공하면 앱은 메인 화면으로 이동합니다.

        요청 스키마는 프로필 수정(MY-02)과 같지만 **별도 오퍼레이션입니다.**
        온보딩은 한 번만 할 수 있어 두 번째 호출이 `409`인 반면,
        프로필 수정은 몇 번이든 가능합니다(ADR-021).

        닉네임은 한글·영문·숫자만 쓸 수 있고 중복될 수 없습니다(ADR-053 · ADR-025).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProfileUpdateRequest' }
      responses:
        '200':
          description: 초기 설정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/MyProfile' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 닉네임 길이 위반 등 검증 실패
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `ONBOARDING_ALREADY_COMPLETED` — 이미 온보딩을 마친 사용자
            `NICKNAME_DUPLICATED` — 이미 쓰이고 있는 닉네임(ADR-025)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /users/me/onboarding/skip:
    post:
      tags: [온보딩]
      operationId: skipOnboarding
      summary: 초기 설정 건너뛰기
      x-user-stories: [ONB-02]
      description: |
        초기 설정을 건너뛰고 바로 메인 화면으로 갑니다.
        온보딩은 완료로 표시되지만 닉네임은 입력받지 않습니다.

        서버가 **`여행자` + 네 자리 숫자** 형태로 기본 닉네임을 만듭니다(ADR-050).
        닉네임은 중복될 수 없으므로(ADR-025) 이미 쓰이는 값이면 다른 숫자로 다시 만듭니다.
        사용자는 나중에 마이페이지에서 바꿀 수 있습니다(MY-02).
      responses:
        '200':
          description: 건너뛰기 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/MyProfile' }
                  meta: { type: 'null' }
        '409':
          description: |
            `ONBOARDING_ALREADY_COMPLETED` — 이미 온보딩을 마친 사용자
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  # ------------------------------------------------------------------
  # 플래너 — 여행 일정(코스)
  # ------------------------------------------------------------------

  /trips:
    post:
      tags: [플래너]
      operationId: createTrip
      summary: 일정 생성
      x-user-stories: [PLAN-01]
      description: |
        여행지(복수 선택 가능)와 기간을 정해 빈 일정을 만듭니다.
        장소는 이후 `POST /trips/{tripId}/places`로 채웁니다.

        여기서 고른 여행지는 이 일정을 커뮤니티에 공유할 때
        게시물 지역 스냅샷의 재료가 됩니다(ADR-003).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TripCreateRequest' }
      responses:
        '200':
          description: 생성 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TripDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 제목 누락, 여행지 미선택 등
            `INVALID_DATE_RANGE` — 시작일이 종료일보다 늦음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `REGION_NOT_FOUND` — 존재하지 않는 지역 코드
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    get:
      tags: [플래너]
      operationId: listTrips
      summary: 일정 목록 조회
      x-user-stories: [PLAN-11]
      description: |
        내가 만든 여행 일정을 지난 여행과 예정된 여행 구분 없이 함께 반환합니다.
        각 항목의 `status`로 앱이 나눠 표시합니다.

        번호 방식 페이지네이션입니다(ADR-011).

        **예정된 여행이 먼저 오고, 그 아래에 지난 여행이 최신순으로 붙습니다**(ADR-040).
        플래너는 준비 중인 여행을 고치러 들어오는 화면이라 그쪽이 위에 있어야 합니다.
        같은 묶음 안에서는 `UPCOMING`·`ONGOING`이 시작일 오름차순, `PAST`가 시작일 내림차순입니다.
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/TripSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }

  /trips/{tripId}:
    get:
      tags: [플래너]
      operationId: getTrip
      summary: 일정 상세 조회
      x-user-stories: [PLAN-03]
      description: |
        여행 기간과 일자별 방문 장소, 이동 순서를 반환합니다.
        연결된 아카이브가 있으면 `linkedArchiveId`에 담깁니다(ADR-001).
      parameters:
        - $ref: '#/components/parameters/TripId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TripDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 남의 일정
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    patch:
      tags: [플래너]
      operationId: updateTrip
      summary: 일정 정보 수정
      x-user-stories: [PLAN-04]
      description: |
        일정의 제목, 기간, 여행지를 변경합니다. 보내지 않은 필드는 그대로 둡니다.
        일정에 담긴 장소를 고치는 것은 `.../places` 오퍼레이션들입니다.

        **기간을 줄여서 갈 곳 없는 장소가 생기면 거부합니다**(ADR-041).
        `details`에 밀려나는 장소를 담아, 앱이 "3일차에 장소 2개가 있습니다"로 안내하고
        사용자가 먼저 옮기거나 지우게 합니다. 서버가 말없이 지우거나 옮기지 않습니다.
      parameters:
        - $ref: '#/components/parameters/TripId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TripUpdateRequest' }
      responses:
        '200':
          description: 수정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TripDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 검증 실패, 또는 기간을 줄여 갈 곳 없는 장소가 생김.
            후자는 `details`에 밀려나는 장소들이 담깁니다(ADR-041)
            `INVALID_DATE_RANGE` — 시작일이 종료일보다 늦음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND` · `REGION_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [플래너]
      operationId: deleteTrip
      summary: 일정 삭제
      x-user-stories: [PLAN-05]
      description: |
        일정을 삭제합니다. **연결된 아카이브는 삭제되지 않고 연결만 끊깁니다**(ADR-001).
        이 일정을 공유한 게시물이 있으면 게시물은 남고 코스 참조만 사라집니다(ADR-002).
      parameters:
        - $ref: '#/components/parameters/TripId'
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /trips/{tripId}/places:
    post:
      tags: [플래너]
      operationId: addTripPlace
      summary: 일정에 장소 추가
      x-user-stories: [PLAN-02, PLAN-08]
      description: |
        일자를 지정해 장소를 일정에 담습니다.
        장소 검색(PLAN-06)에서 고른 경우와 주변 추천(PLAN-08)에서 고른 경우가
        같은 오퍼레이션을 씁니다 — 쓰기 동작이 동일해서 나누면 같은 기능이 두 벌 생깁니다.

        `sortOrder`를 생략하면 해당 일자의 맨 뒤에 붙습니다.
      parameters:
        - $ref: '#/components/parameters/TripId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TripPlaceCreateRequest' }
      responses:
        '200':
          description: 추가 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TripPlace' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — `dayNumber`가 일정 기간을 벗어남
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND` · `PLACE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /trips/{tripId}/places/order:
    put:
      tags: [플래너]
      operationId: reorderTripPlaces
      summary: 방문 순서 변경
      x-user-stories: [PLAN-02, PLAN-04]
      description: |
        일정에 담긴 장소들의 일자와 순서를 한 번에 다시 매깁니다.
        드래그로 순서를 바꾸면 화면이 최종 배치를 통째로 보내고, 서버는 그대로 저장합니다.

        **부분 전송을 허용하지 않습니다.** 일정에 담긴 모든 장소를 보내야 합니다.
        일부만 보내면 나머지의 순서가 어떻게 되어야 하는지 서버가 알 수 없습니다.
      parameters:
        - $ref: '#/components/parameters/TripId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TripPlaceOrderRequest' }
      responses:
        '200':
          description: 순서 변경 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TripDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 일정에 담긴 장소를 모두 보내지 않았거나 `dayNumber`가 기간을 벗어남
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND` · `TRIP_PLACE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /trips/{tripId}/places/{tripPlaceId}:
    patch:
      tags: [플래너]
      operationId: updateTripPlace
      summary: 담긴 장소 수정
      x-user-stories: [PLAN-04]
      description: |
        일정에 담긴 장소 한 건의 일자, 순서, 메모를 바꿉니다.
        여러 건의 순서를 한꺼번에 재배치할 때는 `PUT .../places/order`를 씁니다.
      parameters:
        - $ref: '#/components/parameters/TripId'
        - $ref: '#/components/parameters/TripPlaceId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TripPlaceUpdateRequest' }
      responses:
        '200':
          description: 수정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TripPlace' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — `dayNumber`가 일정 기간을 벗어남
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND` · `TRIP_PLACE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [플래너]
      operationId: removeTripPlace
      summary: 담긴 장소 제거
      x-user-stories: [PLAN-04]
      description: |
        일정에서 장소 한 건을 뺍니다. 관광지 자체(`places`)는 지워지지 않습니다.
        같은 일자에 남은 장소들의 순서는 서버가 다시 채웁니다.
      parameters:
        - $ref: '#/components/parameters/TripId'
        - $ref: '#/components/parameters/TripPlaceId'
      responses:
        '200':
          description: 제거 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND` · `TRIP_PLACE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /trips/{tripId}/nearby-places:
    get:
      tags: [플래너]
      operationId: listNearbyPlaces
      summary: 주변 추천 장소 조회
      x-user-stories: [PLAN-08]
      description: |
        일정에 이미 담긴 장소들을 기준으로 주변 관광지를 추천합니다.
        콘텐츠랩의 연관 관광지·위치기반 관광정보를 사용합니다.

        여기서 고른 장소를 일정에 넣는 것은 `POST /trips/{tripId}/places`입니다.

        **기준 일자에 담긴 장소들을 중심으로 반경 5km 안을 찾고, 이미 담은 장소는 뺍니다**(ADR-042).
        `dayNumber`를 생략하면 일정 전체의 장소를 기준으로 합니다.
        그 날 동선 안에서 추천이 나오므로 넣어도 이동이 크게 늘지 않습니다.
      parameters:
        - $ref: '#/components/parameters/TripId'
        - name: dayNumber
          in: query
          required: false
          description: |
            기준으로 삼을 일자. 그 날 담긴 장소들의 주변을 찾습니다.
            생략하면 일정 전체의 장소를 기준으로 합니다(ADR-042)
          schema: { type: integer, minimum: 1 }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PlaceSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '503':
          description: |
            `TOUR_API_UNAVAILABLE` — 콘텐츠랩 장애 또는 호출 한도 초과
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /trips/{tripId}/auto-arrange:
    post:
      tags: [플래너]
      operationId: autoArrangeTrip
      summary: AI 일정 자동 배치 (미리보기)
      x-user-stories: [PLAN-09]
      description: |
        일정에 담긴 장소들을 이동 거리가 짧아지는 순서로 다시 배치한 **제안을 반환합니다.**
        **저장하지 않습니다**(ADR-031). 일정은 그대로 있습니다.

        제안이 마음에 들면 앱이 응답의 `items`를 그대로
        `PUT /trips/{tripId}/places/order`에 보내 적용합니다.
        요청 본문 모양이 같아서 그대로 전달하면 됩니다.
        마음에 들지 않으면 아무것도 하지 않으면 됩니다 — 되돌릴 것이 없습니다.

        **장소를 추천해 주지는 않습니다** — 순서 정리까지가 이 오퍼레이션의 범위입니다(ADR-020).
        얼마나 나아지는지는 `currentDistanceMeters`와 `proposedDistanceMeters`로 비교합니다.

        **좌표 기반 자체 계산입니다**(ADR-032). 외부 모델을 호출하지 않으므로
        비용이 들지 않고, 같은 입력에는 항상 같은 결과가 나오며, 외부 장애로 실패하지 않습니다.
      parameters:
        - $ref: '#/components/parameters/TripId'
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AutoArrangeRequest' }
      responses:
        '200':
          description: 제안 생성 완료. 일정은 아직 바뀌지 않았습니다
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/AutoArrangeResult' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 배치할 장소가 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND` · `PLACE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /trips/{tripId}/route:
    get:
      tags: [플래너]
      operationId: getTripRoute
      summary: 코스 시각화 데이터 조회
      x-user-stories: [PLAN-10]
      description: |
        일자별로 방문 순서대로 정렬한 좌표 목록과, 지도를 맞출 경계 상자를 반환합니다.
        앱은 이 좌표를 이어 동선을 그립니다.

        일정 상세(PLAN-03)에도 좌표가 들어 있지만, 지도만 그릴 때
        장소 설명과 메모까지 받는 것은 낭비라 조회를 나눴습니다.

        **이동 거리는 직선 거리 합입니다**(ADR-043). 실제 도로 거리가 아닙니다.
        외부 경로 API를 붙이지 않으므로 비용이 들지 않고 즉시 계산됩니다.
        자동 배치(ADR-032)도 같은 기준을 쓰므로 두 화면의 숫자가 일치합니다.
      parameters:
        - $ref: '#/components/parameters/TripId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TripRoute' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /posts/{postId}/course/import:
    post:
      tags: [플래너]
      operationId: importSharedCourse
      summary: 공유된 코스 가져오기
      x-user-stories: [PLAN-12]
      description: |
        커뮤니티 게시물에 공유된 코스를 내 일정으로 복사합니다.
        복사본의 `originTripId`에 원본 일정 식별자가 남습니다(ADR-005).

        원본이 나중에 수정되거나 삭제돼도 복사본은 영향을 받지 않습니다.

        **`startDate`를 받아 그날부터 일차를 다시 매깁니다**(ADR-048).
        원본이 3박 4일이면 내 시작일부터 3박 4일로 잡힙니다.
        생략하면 기간 없는 초안으로 들어오고, 나중에 `PATCH /trips/{tripId}`로 채웁니다 —
        원본 날짜를 그대로 복사하면 가져오자마자 지난 여행으로 분류됩니다.
      parameters:
        - name: postId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                startDate:
                  type: [string, 'null']
                  format: date
                  description: |
                    복사본의 여행 시작일. 원본의 여행 일수만큼 여기서부터 다시 매깁니다.
                    생략하면 기간 없는 초안으로 들어옵니다(ADR-048)
                title:
                  type: [string, 'null']
                  maxLength: 60
                  description: 복사본 제목. 생략하면 원본 제목을 씁니다
      responses:
        '200':
          description: 복사 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TripDetail' }
                  meta: { type: 'null' }
        '404':
          description: |
            `POST_NOT_FOUND` — 게시물이 없거나 삭제됨
            `TRIP_NOT_FOUND` — 게시물에 공유된 코스가 없거나 원본이 삭제됨(ADR-002)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }


  # ------------------------------------------------------------------
  # 장소 — 콘텐츠랩 관광정보
  # ------------------------------------------------------------------

  /places/search:
    get:
      tags: [장소]
      operationId: searchPlaces
      summary: 장소 검색
      x-user-stories: [PLAN-06]
      description: |
        지역이나 장소명으로 관광지를 찾습니다.
        콘텐츠랩에서 받아 `places` 테이블에 적재한 데이터를 조회하며,
        상세 정보는 별도로 짧은 TTL 캐시를 둡니다(ADR-004).

        `keyword`와 `regionCode` 중 최소 하나는 있어야 합니다.
        둘 다 없으면 전국 관광지 26만 건을 훑게 됩니다.
      parameters:
        - name: keyword
          in: query
          required: false
          description: 장소명 부분 일치
          schema: { type: string, minLength: 1, maxLength: 50 }
        - name: regionCode
          in: query
          required: false
          description: 시/도 또는 시군구 코드. 시/도를 주면 하위 시군구까지 포함합니다
          schema: { type: string }
        - name: contentTypeId
          in: query
          required: false
          description: 관광지·음식점·숙박 등 콘텐츠랩 분류 코드
          schema: { type: string }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 검색 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PlaceSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }
        '400':
          description: |
            `INVALID_REQUEST` — `keyword`와 `regionCode`가 모두 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `REGION_NOT_FOUND` — 존재하지 않는 지역 코드
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '503':
          description: |
            `TOUR_API_UNAVAILABLE` — 적재되지 않은 데이터를 콘텐츠랩에서 가져오는 중 장애
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /places/{placeId}:
    get:
      tags: [장소]
      operationId: getPlace
      summary: 장소 상세 조회
      x-user-stories: [PLAN-07]
      description: |
        일정에 넣기 전에 확인할 관광지 정보를 반환합니다.
        이름, 위치, 이미지, 주소, 연락처, 소개글이 포함됩니다.

        운영 시간과 휴무일은 **콘텐츠랩이 준 문자열을 그대로** 내려보냅니다(ADR-049).
        서버가 요일별 구조로 파싱하지 않습니다 — 분류마다 형식이 제각각이라
        파싱하면 틀린 정보를 보여줄 위험이 있습니다.
        정보가 없는 장소는 `null`이고, 앱은 그럴 때 항목을 감춥니다.
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/PlaceDetail' }
                  meta: { type: 'null' }
        '404':
          description: |
            `PLACE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '503':
          description: |
            `TOUR_API_UNAVAILABLE` — 상세 정보를 콘텐츠랩에서 가져오는 중 장애
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }


  # ------------------------------------------------------------------
  # 지도 — 지역 방문 인증
  # ------------------------------------------------------------------

  /region-visits:
    post:
      tags: [지도]
      operationId: verifyRegionVisit
      summary: 여행 지역 인증
      x-user-stories: [MAP-01, MAP-04]
      description: |
        현재 위치가 인증하려는 지역 안에 있는지 확인하고 방문 기록을 남깁니다.
        인증에 성공하면 정원 보상이 함께 처리되며(GARDEN-02),
        결과는 응답의 `rewardStatus`와 `POST /region-visits` 응답의 `visitId`로 확인합니다.

        **MAP-04(인증 실패)는 이 오퍼레이션의 오류 응답입니다.**
        실패하는 별도 엔드포인트를 만들 수 없어 여기에 함께 적었습니다.
        다만 "위치 권한을 허용하지 않은" 경우는 앱에서 막히므로 서버까지 오지 않습니다.

        좌표에서 지역을 판정하는 방식은 콘텐츠랩 위치기반 관광정보의 `areacode`를 채택합니다(ADR-004).
        요청에 `regionCode`를 함께 보내면 서버 판정 결과와 대조해 다르면 거절합니다.

        **쿨다운은 인증을 막지 않습니다**(ADR-038).
        7일 안에 같은 지역을 다시 인증해도 방문 기록은 남고 `rewardStatus`만 `COOLDOWN`이 됩니다.
        부산에 세 번 갔으면 세 번 다 기록되고, 식물만 한 번 자랍니다.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RegionVisitRequest' }
      responses:
        '200':
          description: 인증 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/RegionVisitResult' }
                  meta: { type: 'null' }
        '400':
          description: |
            `LOCATION_MISMATCH` — 보낸 좌표가 인증하려는 지역에 속하지 않음
            `LOCATION_ACCURACY_TOO_LOW` — 위치 정확도가 100m를 넘음(ADR-016)
            `REGION_NOT_DETERMINED` — 좌표로 지역을 판정하지 못함
            `INVALID_REQUEST` — 좌표 형식 오류 등
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `REGION_NOT_FOUND` — 존재하지 않는 지역 코드
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '503':
          description: |
            `TOUR_API_UNAVAILABLE` — 지역 판정에 쓰는 콘텐츠랩 조회가 실패함
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /users/me/regions:
    get:
      tags: [지도]
      operationId: listMyRegions
      summary: 방문 지역 확인
      x-user-stories: [MAP-02]
      description: |
        지도를 칠하는 데 필요한 데이터입니다.
        **방문한 지역만이 아니라 전체 지역을 반환하고 `visited`로 구분합니다** —
        MAP-02의 결과가 "방문한 지역이 **시각적으로 구분되어** 표시된다"이므로
        앱이 미방문 지역까지 알아야 색을 나눠 칠할 수 있습니다.

        페이지를 나누지 않습니다. 시/도는 17개, 시군구도 250개 수준이라
        한 번에 받아 지도에 칠하는 편이 낫습니다.
      parameters:
        - name: level
          in: query
          required: false
          description: 반환할 지역 단위
          schema: { type: string, enum: [SIDO, SIGUNGU], default: SIDO }
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/RegionVisitStatus' }
                  meta: { type: 'null' }

  /regions/{regionCode}:
    get:
      tags: [지도]
      operationId: getRegion
      summary: 지역 상세 정보 확인
      x-user-stories: [MAP-03]
      description: |
        지도에서 지역을 눌렀을 때 보여줄 정보입니다.
        지역 이름과 중심 좌표, 내 방문 여부, 그 지역에서 해금할 수 있는 정원 오브젝트를 반환합니다.

        그 지역의 관광지 목록은 `GET /regions/{regionCode}/places`에서 따로 받습니다.
      parameters:
        - $ref: '#/components/parameters/RegionCode'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/RegionDetail' }
                  meta: { type: 'null' }
        '404':
          description: |
            `REGION_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /regions/{regionCode}/places:
    get:
      tags: [지도]
      operationId: listRegionPlaces
      summary: 지역별 관광 정보 조회
      x-user-stories: [MAP-03]
      description: |
        지역에 속한 관광지와 음식점을 분류별로 훑어봅니다.
        여기서 고른 장소를 일정에 넣는 것은 `POST /trips/{tripId}/places`입니다.

        > **`GET /places/search?regionCode=`와 결과가 겹칩니다.**
        > 검색(PLAN-06)은 키워드 중심이고 이 오퍼레이션은 지도에서 지역을 눌러 훑어보는
        > 진입점이라 스토리를 나눠 두었지만, 반환하는 데이터는 같습니다.
        > 두 화면의 요구가 끝까지 같으면 나중에 하나로 합칠 수 있습니다.
      parameters:
        - $ref: '#/components/parameters/RegionCode'
        - name: contentTypeId
          in: query
          required: false
          description: 관광지·음식점·숙박 등 콘텐츠랩 분류 코드
          schema: { type: string }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PlaceSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }
        '404':
          description: |
            `REGION_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '503':
          description: |
            `TOUR_API_UNAVAILABLE`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  # ------------------------------------------------------------------
  # 정원
  # ------------------------------------------------------------------

  /garden:
    get:
      tags: [정원]
      operationId: getMyGarden
      summary: 정원 조회
      x-user-stories: [GARDEN-01]
      description: |
        정원에 배치된 식물과 오브젝트, 각각의 성장 단계를 반환합니다.
        아직 해금하지 않은 것은 포함하지 않습니다 —
        어느 지역에서 무엇을 얻을 수 있는지는 `GET /regions/{regionCode}`에서 봅니다.

        **정원은 사용자당 하나입니다**(ADR-037). 어느 지역에서 얻었든 한 정원에 모입니다.
        지역별로 나누지 않으므로 이 경로에 지역 구분이 없습니다.
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Garden' }
                  meta: { type: 'null' }

  /region-visits/{visitId}/reward:
    get:
      tags: [정원]
      operationId: getVisitReward
      summary: 지역 보상 확인
      x-user-stories: [GARDEN-02]
      description: |
        방문 인증 한 건이 정원에 무엇을 남겼는지 확인합니다.
        처음 방문한 지역이면 오브젝트가 해금되고, 이미 해금한 지역이면 성장합니다.
        7일 쿨다운 안에 다시 인증했다면 `COOLDOWN`이며 아무것도 바뀌지 않습니다(ADR-018).

        인증 직후 결과는 `POST /region-visits` 응답에도 들어 있습니다.
        이 오퍼레이션은 보상 연출을 다시 보거나, 인증 응답을 놓쳤을 때 쓰기 위한 것입니다.
      parameters:
        - $ref: '#/components/parameters/VisitId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/VisitReward' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 남의 방문 기록
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `REGION_VISIT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }


  # ------------------------------------------------------------------
  # 미디어
  # ------------------------------------------------------------------

  /media/upload-urls:
    post:
      tags: [미디어]
      operationId: issueMediaUploadUrls
      summary: 업로드 URL 발급
      x-user-stories: [MEDIA-01]
      description: |
        올릴 파일의 정보를 보내면 스토리지에 직접 올릴 수 있는 URL을 발급합니다.
        **파일은 서버를 거치지 않고 앱이 스토리지로 바로 보냅니다**(ADR-023).

        형식과 용량 검증은 여기서 합니다 — URL을 내주기 전에 막아야
        잘못된 파일이 스토리지에 올라가지 않습니다.

        **영상은 해상도와 길이를 함께 보내야 합니다**(ADR-052).
        서버가 S3의 영상을 열어보지 않으므로 앱이 알려주지 않으면 알 수 없고,
        해상도를 모르면 아카이브에 배치할 때 영상이 찌그러집니다.

        업로드 순서는 이렇습니다.
        1. 이 오퍼레이션으로 `mediaId`와 `uploadUrl`을 받습니다
        2. 앱이 `uploadUrl`에 파일을 `PUT`으로 올립니다 (이 API가 아닙니다)
        3. `POST /media/complete`로 완료를 알립니다

        발급된 URL은 `expiresAt`이 지나면 쓸 수 없습니다. 만료되면 다시 발급받습니다.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [files]
              properties:
                files:
                  type: array
                  minItems: 1
                  maxItems: 20
                  items: { $ref: '#/components/schemas/MediaUploadRequest' }
      responses:
        '200':
          description: 발급 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    description: 요청에 보낸 순서와 같은 순서로 반환합니다
                    items: { $ref: '#/components/schemas/MediaUploadTicket' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_MEDIA_FORMAT` — 사진 JPEG·PNG·HEIC·WEBP / 영상 MP4·MOV 이외의 형식
            `MEDIA_TOO_LARGE` — 사진 10MB 또는 영상 200MB 초과
            `INVALID_REQUEST` — 파일이 없거나 20개를 넘음, 또는 영상인데 `width`·`height`·`durationMs`가 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /media/complete:
    post:
      tags: [미디어]
      operationId: completeMediaUpload
      summary: 업로드 완료 통보
      x-user-stories: [MEDIA-01]
      description: |
        스토리지에 올리기를 마쳤음을 알립니다.
        서버는 파일이 실제로 있는지 확인하고 `status`를 `READY`로 바꿉니다.

        **사진**은 서버가 EXIF에서 촬영 시각·좌표·해상도를 읽습니다.
        **영상**은 서버가 열어보지 않습니다 — 해상도와 길이는 업로드 URL을 받을 때
        앱이 함께 보낸 값을 그대로 씁니다(ADR-052).

        **`READY`가 된 `mediaId`만 아카이브·타임캡슐에 담을 수 있습니다.**

        EXIF가 없거나 지워진 파일이면 `takenAt`·`lat`·`lng`가 `null`이 됩니다.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mediaIds]
              properties:
                mediaIds:
                  type: array
                  minItems: 1
                  maxItems: 20
                  items: { type: integer, format: int64 }
      responses:
        '200':
          description: 처리 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/Media' }
                  meta: { type: 'null' }
        '400':
          description: |
            `MEDIA_UPLOAD_INCOMPLETE` — 스토리지에 파일이 올라오지 않았습니다. `details.mediaIds`에 해당 항목
            `INVALID_REQUEST` — 목록이 비었거나 20개를 넘음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 남이 발급받은 `mediaId`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `MEDIA_NOT_FOUND` — 발급받은 적 없는 `mediaId`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `DUPLICATE_REQUEST` — 이미 완료 처리된 항목
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  # ------------------------------------------------------------------
  # 타임캡슐
  # ------------------------------------------------------------------

  /time-capsules:
    post:
      tags: [타임캡슐]
      operationId: createTimeCapsule
      summary: 타임캡슐 생성
      x-user-stories: [TC-01]
      description: |
        글과 사진을 담고 해제 조건을 걸어 봉인합니다.
        만든 직후 상태는 `SEALED`이며, 본인도 조건이 충족되기 전에는 내용을 볼 수 없습니다.

        해제 조건은 **날짜 또는 위치 중 하나만** 지정합니다.
        둘 다 보내거나 둘 다 비우면 `INVALID_UNLOCK_CONDITION`입니다.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TimeCapsuleCreateRequest' }
      responses:
        '200':
          description: 봉인 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TimeCapsuleDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_UNLOCK_CONDITION` — 해제 조건이 날짜·위치 중 하나로 정해지지 않음
            `INVALID_REQUEST` — 제목 누락, 과거 날짜 지정 등
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `MEDIA_NOT_FOUND` — 담으려는 사진·영상이 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    get:
      tags: [타임캡슐]
      operationId: listTimeCapsules
      summary: 타임캡슐 목록 조회
      x-user-stories: [TC-02]
      description: |
        봉인된 것과 해제된 것을 **함께** 반환합니다. 각 항목의 `status`로 앱이 나눠 표시합니다.
        열어본 것만 따로 보려면 `GET /time-capsules/opened`를 씁니다.

        커서 방식 페이지네이션입니다(ADR-011).
        내용은 포함하지 않습니다 — 목록에서 봉인된 캡슐의 내용이 새면 안 됩니다.
      parameters:
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/TimeCapsuleSummary' }
                  meta: { $ref: '#/components/schemas/CursorMeta' }

  /time-capsules/opened:
    get:
      tags: [타임캡슐]
      operationId: listOpenedTimeCapsules
      summary: 해제 기록 조회
      x-user-stories: [TC-06]
      description: |
        열어본 타임캡슐만 최근에 연 순서로 반환합니다.
        내용을 다시 보려면 항목을 골라 `GET /time-capsules/{capsuleId}`를 호출합니다.
      parameters:
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/TimeCapsuleSummary' }
                  meta: { $ref: '#/components/schemas/CursorMeta' }

  /time-capsules/{capsuleId}:
    get:
      tags: [타임캡슐]
      operationId: getTimeCapsule
      summary: 해제 조건 확인 · 내용 다시 보기
      x-user-stories: [TC-03, TC-06]
      description: |
        해제 조건과 현재 상태를 반환합니다.

        **내용은 상태에 따라 다릅니다.**
        `SEALED`·`UNLOCKABLE`이면 `content`와 `media`가 `null`·빈 배열입니다 —
        아직 열지 않은 캡슐의 내용이 여기서 새면 타임캡슐이 성립하지 않습니다.
        `OPENED`가 되면 내용이 함께 내려와, 열어본 캡슐을 다시 볼 때(TC-06) 이 오퍼레이션을 씁니다.
      parameters:
        - $ref: '#/components/parameters/CapsuleId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TimeCapsuleDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 남의 타임캡슐
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TIME_CAPSULE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [타임캡슐]
      operationId: deleteTimeCapsule
      summary: 타임캡슐 삭제
      x-user-stories: [TC-07]
      description: |
        타임캡슐을 삭제합니다. 본인 것만 걸린 데이터라 상태 전환이 아니라 **실제로 지웁니다**(ADR-007).
        담긴 사진·영상(`media`)은 다른 곳에서도 쓸 수 있으므로 함께 지우지 않습니다.

        봉인 상태에서도 삭제할 수 있습니다 — 열어보지 않고 없애는 것도 사용자의 선택입니다.
      parameters:
        - $ref: '#/components/parameters/CapsuleId'
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TIME_CAPSULE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /time-capsules/{capsuleId}/open:
    post:
      tags: [타임캡슐]
      operationId: openTimeCapsule
      summary: 타임캡슐 열기
      x-user-stories: [TC-05]
      description: |
        해제 가능 상태(`UNLOCKABLE`)인 캡슐을 열고 내용을 반환합니다.
        상태가 `OPENED`로 바뀌며 `openedAt`이 기록됩니다.

        아직 조건이 충족되지 않았으면 `CAPSULE_NOT_UNLOCKABLE`입니다.
        이미 연 캡슐은 이 오퍼레이션 대신 `GET /time-capsules/{capsuleId}`로 다시 봅니다.
      parameters:
        - $ref: '#/components/parameters/CapsuleId'
      responses:
        '200':
          description: 열기 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/TimeCapsuleDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TIME_CAPSULE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `CAPSULE_NOT_UNLOCKABLE` — 아직 해제 조건이 충족되지 않음. `details.unlockType`
            `CAPSULE_ALREADY_OPENED` — 이미 연 캡슐
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /time-capsules/unlock-check:
    post:
      tags: [타임캡슐]
      operationId: checkLocationUnlock
      summary: 위치 해제 조건 확인
      x-user-stories: [TC-04]
      description: |
        현재 위치를 보고해 위치 조건 캡슐이 열릴 수 있는지 판정합니다.
        조건을 만족한 캡슐은 `UNLOCKABLE`로 바뀌고 알림이 발송됩니다(NOTI-01).

        **날짜 조건 캡슐에는 이 오퍼레이션이 필요 없습니다.**
        서버 배치가 매일 확인해 상태를 바꾸고 알림을 보냅니다.
        TC-04의 두 조건 중 위치 쪽만 클라이언트가 알려줘야 판정할 수 있어 이 오퍼레이션이 있습니다.

        **앱이 켜질 때 한 번 호출하는 방식입니다**(ADR-026).
        백그라운드 지오펜싱을 쓰지 않으므로 위치 권한은 "앱 사용 중에만"으로 충분하고
        배터리 소모가 없습니다. 대신 **그 장소에 가더라도 앱을 열어야 알림이 옵니다.**

        위치 조건 캡슐이 하나도 없으면 앱이 호출할 필요가 없습니다 —
        `GET /time-capsules`의 `unlockType`으로 미리 확인할 수 있습니다.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [lat, lng]
              properties:
                lat: { type: number, format: double }
                lng: { type: number, format: double }
      responses:
        '200':
          description: |
            판정 완료. 이번 호출로 새로 열 수 있게 된 캡슐만 반환합니다.
            해당하는 캡슐이 없으면 빈 배열입니다
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/TimeCapsuleSummary' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 좌표 형식 오류
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }


  # ------------------------------------------------------------------
  # 아카이브
  # ------------------------------------------------------------------

  /archives:
    post:
      tags: [아카이브]
      operationId: createArchive
      summary: 아카이브 생성
      x-user-stories: [ARCH-01]
      description: |
        이름·테마·대표 색상을 정해 빈 아카이브를 만듭니다. 사진은 이후 ARCH-06으로 채웁니다.
        여행 일정을 함께 연결할 수 있지만 필수가 아닙니다 —
        계획 없이 다녀와 사진만 정리하는 경우가 있어 `tripId`는 생략 가능합니다(ADR-001).

        **여행 기간은 입력받지 않습니다.** 담긴 사진의 촬영 시각에서 서버가 계산합니다(ADR-030).
        만든 직후에는 사진이 없으므로 기간이 `null`이고, 사진을 담으면 채워집니다.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ArchiveCreateRequest' }
      responses:
        '200':
          description: 생성 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 이름 누락, 테마 값 오류 등
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 남의 여행 일정을 연결하려 함
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND` — 연결하려는 일정이 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `TRIP_ARCHIVE_ALREADY_LINKED` — 그 일정에 이미 다른 아카이브가 연결됨(ADR-001)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    get:
      tags: [아카이브]
      operationId: listArchives
      summary: 아카이브 목록 조회
      x-user-stories: [ARCH-03]
      description: |
        내가 만든 아카이브와 공동 편집자로 참여 중인 아카이브를 함께 반환합니다.
        각 항목의 `myRole`로 구분합니다.

        커서 방식 페이지네이션입니다(ADR-011).
        이름이나 기간으로 좁혀 찾을 때는 `GET /archives/search`를 씁니다.
      parameters:
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/ArchiveSummary' }
                  meta: { $ref: '#/components/schemas/CursorMeta' }

  /archives/search:
    get:
      tags: [아카이브]
      operationId: searchArchives
      summary: 아카이브 검색
      x-user-stories: [ARCH-04]
      description: |
        이름 또는 여행 기간으로 내 아카이브를 찾습니다.
        `keyword`와 기간(`from`·`to`) 중 최소 하나는 있어야 합니다.

        기간을 주면 **아카이브의 여행 기간이 그 범위와 겹치는** 것을 반환합니다.
        완전히 포함되는 것만 찾으면 "7월 한 달" 검색에 6월 30일~7월 2일 여행이 빠집니다.

        아카이브의 기간은 담긴 사진의 촬영 시각에서 계산합니다(ADR-030).
        **촬영 정보가 없는 사진만 담긴 아카이브는 기간 검색에 잡히지 않습니다.**
        이름으로는 찾을 수 있습니다.
      parameters:
        - name: keyword
          in: query
          required: false
          description: 아카이브 이름 부분 일치
          schema: { type: string, minLength: 1, maxLength: 60 }
        - name: from
          in: query
          required: false
          schema: { type: string, format: date }
        - name: to
          in: query
          required: false
          schema: { type: string, format: date }
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 검색 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/ArchiveSummary' }
                  meta: { $ref: '#/components/schemas/CursorMeta' }
        '400':
          description: |
            `INVALID_REQUEST` — 검색 조건이 하나도 없음
            `INVALID_DATE_RANGE` — `from`이 `to`보다 늦음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}:
    get:
      tags: [아카이브]
      operationId: getArchive
      summary: 아카이브 상세 조회
      x-user-stories: [ARCH-05]
      description: |
        배치된 사진·영상과 레이아웃, 공동 편집자 목록을 반환합니다.

        공동 편집이 종료된 아카이브도 참여자는 **조회할 수 있습니다**(ADR-017).
        `COLLABORATION_CLOSED`는 편집 요청에만 발생합니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 소유자도 공동 편집자도 아님
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }


    patch:
      tags: [아카이브]
      operationId: updateArchive
      summary: 아카이브 정보 수정
      x-user-stories: [ARCH-02]
      description: |
        아카이브의 이름·테마·대표 색상을 바꿉니다. 보내지 않은 필드는 그대로 둡니다.
        배치된 사진을 고치는 것은 `.../items` 오퍼레이션들입니다.

        **여행 기간은 여기서 바꿀 수 없습니다** — 사진 촬영 시각에서 계산되는 값입니다(ADR-030).

        **테마를 바꾸면 기존 배치가 새 테마의 캔버스와 맞지 않을 수 있습니다.**
        이 문서는 배치를 그대로 두고 앱이 다시 조정하게 합니다 — 서버가 임의로 재배치하면
        사용자가 공들여 맞춘 구성이 말없이 흐트러집니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ArchiveUpdateRequest' }
      responses:
        '200':
          description: 수정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 이름 길이 위반, 테마 값 오류 등
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` · `NOT_COLLABORATOR` · `COLLABORATION_CLOSED`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [아카이브]
      operationId: deleteArchive
      summary: 아카이브 삭제
      x-user-stories: [ARCH-09]
      description: |
        아카이브를 삭제합니다. **연결된 여행 일정은 삭제되지 않고 연결만 끊깁니다**(ADR-001).
        이 아카이브를 공유한 게시물이 있으면 게시물은 남고 참조만 사라집니다(ADR-002).

        소유자만 삭제할 수 있습니다. 공동 편집자는 참여만 해제할 수 있습니다.
        담긴 사진·영상은 다른 곳에서도 쓸 수 있어 함께 지우지 않습니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 소유자가 아님
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/items:
    post:
      tags: [아카이브]
      operationId: addArchiveItems
      summary: 사진·영상 추가
      x-user-stories: [ARCH-06]
      description: |
        먼저 업로드를 마쳐 `READY` 상태가 된 사진·영상을 아카이브에 배치합니다(ADR-023).
        `layout`을 생략하면 서버가 테마의 기본 배치를 채웁니다.

        여러 장을 한 번에 담을 수 있습니다. 순서는 보낸 순서대로 뒤에 붙습니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ArchiveItemsAddRequest' }
      responses:
        '200':
          description: 추가 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/ArchiveItem' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 담을 항목이 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` · `NOT_COLLABORATOR` — 편집 권한 없음
            `COLLABORATION_CLOSED` — 공동 편집이 종료된 아카이브(ADR-017)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND` · `MEDIA_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/items/layout:
    put:
      tags: [아카이브]
      operationId: updateArchiveLayout
      summary: 레이아웃·순서 일괄 변경
      x-user-stories: [ARCH-07]
      description: |
        배치와 순서를 한 번에 다시 정합니다.
        화면이 편집을 마친 최종 배치를 통째로 보내고, 서버는 그대로 저장합니다.

        **부분 전송을 허용하지 않습니다.** 아카이브의 모든 항목을 보내야 합니다 —
        일부만 보내면 나머지의 순서가 어떻게 되어야 하는지 서버가 알 수 없습니다.
        항목 하나만 고칠 때는 `PATCH .../items/{itemId}`를 씁니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ArchiveLayoutRequest' }
      responses:
        '200':
          description: 변경 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 아카이브의 모든 항목을 보내지 않았음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` · `NOT_COLLABORATOR` · `COLLABORATION_CLOSED`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND` · `ARCHIVE_ITEM_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/items/{itemId}:
    patch:
      tags: [아카이브]
      operationId: updateArchiveItem
      summary: 배치된 항목 하나 수정
      x-user-stories: [ARCH-06, ARCH-07]
      description: |
        항목 하나의 배치·순서·캡션을 바꿉니다. 실시간 공동 편집(ARCH-12)에서
        한 사람이 사진 한 장을 옮길 때 보내는 변경 단위이기도 합니다.

        캡션은 사진 한 장에 붙이는 짧은 글입니다(ADR-029).
        `layout`만 바꿀 때와 같은 오퍼레이션을 쓰므로, 글만 고칠 때는 `caption`만 보내면 됩니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
        - $ref: '#/components/parameters/ArchiveItemId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ArchiveItemUpdateRequest' }
      responses:
        '200':
          description: 수정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveItem' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 배치 값이 범위를 벗어남
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` · `NOT_COLLABORATOR` · `COLLABORATION_CLOSED`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND` · `ARCHIVE_ITEM_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [아카이브]
      operationId: removeArchiveItem
      summary: 배치된 항목 제거
      x-user-stories: [ARCH-06]
      description: |
        아카이브에서 항목 하나를 뺍니다. 사진·영상 자체(`media`)는 지워지지 않습니다.
        남은 항목들의 순서는 서버가 다시 채웁니다.

        대표 사진으로 지정된 항목을 빼면 대표 사진이 `null`이 됩니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
        - $ref: '#/components/parameters/ArchiveItemId'
      responses:
        '200':
          description: 제거 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` · `NOT_COLLABORATOR` · `COLLABORATION_CLOSED`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND` · `ARCHIVE_ITEM_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/cover:
    put:
      tags: [아카이브]
      operationId: setArchiveCover
      summary: 대표 사진 지정
      x-user-stories: [ARCH-08]
      description: |
        아카이브에 배치된 사진 중 하나를 대표로 삼습니다.
        목록과 커뮤니티 게시물의 썸네일로 쓰입니다.

        영상은 대표로 지정할 수 없습니다 — ARCH-08이 "사진 중 하나"입니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [itemId]
              properties:
                itemId:
                  type: integer
                  format: int64
                  description: 아카이브에 배치된 항목의 식별자
      responses:
        '200':
          description: 지정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 영상을 대표로 지정하려 함
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` · `NOT_COLLABORATOR` · `COLLABORATION_CLOSED`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND` · `ARCHIVE_ITEM_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/trip:
    put:
      tags: [아카이브]
      operationId: linkTripToArchive
      summary: 여행 일정 연결
      x-user-stories: [ARCH-17]
      description: |
        아카이브에 여행 일정을 잇습니다. 이미 다른 일정이 연결돼 있으면 교체합니다.
        일정 하나에는 아카이브 하나만 붙습니다(ADR-001).
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tripId]
              properties:
                tripId: { type: integer, format: int64 }
      responses:
        '200':
          description: 연결 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 남의 아카이브이거나 남의 일정
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND` · `TRIP_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `TRIP_ARCHIVE_ALREADY_LINKED` — 그 일정에 이미 다른 아카이브가 연결됨
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [아카이브]
      operationId: unlinkTripFromArchive
      summary: 여행 일정 연결 해제
      x-user-stories: [ARCH-17]
      description: |
        연결만 끊습니다. **아카이브도 일정도 삭제되지 않습니다**(ADR-001).
        연결된 일정이 없으면 아무 일도 일어나지 않고 `200`입니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      responses:
        '200':
          description: 해제 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/duplicate:
    post:
      tags: [아카이브]
      operationId: duplicateArchive
      summary: 공동 편집 아카이브 복제
      x-user-stories: [ARCH-15]
      description: |
        함께 만든 아카이브를 **내용까지 통째로** 내 아카이브로 복사합니다.
        사진·영상·배치가 모두 복사되며, `originArchiveId`에 원본이 남습니다(ADR-005).

        커뮤니티에서 가져오는 ARCH-16과 다릅니다 —
        저쪽은 **틀만** 복제하고 사진은 가져오지 않습니다.
        내가 함께 만든 기록이라 내용까지 가져갈 수 있는 것입니다.

        **종료 전에도 복제할 수 있습니다**(ADR-039).
        ARCH-15의 "종료 후"는 화면 흐름이지 서버가 막을 제약이 아니라고 보았습니다.
        편집 중에 빠지려는 사람(ARCH-13)도 나가기 전에 복제해 둘 수 있습니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: [string, 'null']
                  maxLength: 60
                  description: 복사본 이름. 생략하면 원본 이름을 씁니다
      responses:
        '200':
          description: 복제 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` · `NOT_COLLABORATOR` — 이 아카이브에 참여한 적이 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /posts/{postId}/archive/import:
    post:
      tags: [아카이브]
      operationId: importSharedArchive
      summary: 공유 아카이브 가져오기
      x-user-stories: [ARCH-16]
      description: |
        커뮤니티 게시물에 공유된 아카이브의 **틀만** 내 아카이브로 복제합니다.

        가져오는 것: 테마, 대표 색상, 항목들의 배치와 순서
        가져오지 않는 것: **사진·영상, 지역, 캡션**

        빈 자리만 있는 아카이브가 만들어지고 내 사진으로 채우게 됩니다.
        `originArchiveId`에 원본이 남습니다(ADR-005).
      parameters:
        - $ref: '#/components/parameters/PostId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title:
                  type: string
                  minLength: 1
                  maxLength: 60
                  description: 내 아카이브 이름. ARCH-16에서 사용자가 입력합니다
      responses:
        '200':
          description: 복제 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 이름 누락
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `POST_NOT_FOUND` — 게시물이 없거나 삭제됨
            `ARCHIVE_NOT_FOUND` — 게시물에 공유된 아카이브가 없거나 원본이 삭제됨(ADR-002)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  # ------------------------------------------------------------------
  # 공동 편집
  # ------------------------------------------------------------------

  /archives/{archiveId}/collaborators:
    post:
      tags: [공동 편집]
      operationId: inviteCollaborator
      summary: 공동 편집 초대
      x-user-stories: [ARCH-10]
      description: |
        아카이브에 함께 만들 사람을 초대합니다.
        초대받은 사용자에게 알림이 갑니다(NOTI-02).
        상대가 수락해야 편집 권한이 생깁니다(ARCH-11).

        아카이브의 `collaborationStatus`가 `NONE`이면 이 호출로 `OPEN`이 됩니다.

        초대할 사람은 `GET /users/search`로 닉네임을 검색해 고릅니다(ADR-024).
        링크·코드로 초대하는 방식은 채택하지 않았으므로
        **동행자가 이미 에버가든에 가입해 있어야 초대할 수 있습니다.**
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [userId]
              properties:
                userId: { type: integer, format: int64 }
      responses:
        '200':
          description: 초대 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Collaborator' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 소유자만 초대할 수 있습니다
            `COLLABORATION_CLOSED` — 이미 종료된 공동 편집
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND` · `USER_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `ALREADY_INVITED` — 이미 초대한 사용자
            `ALREADY_COLLABORATOR` — 이미 참여 중인 사용자
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/collaborators/me/accept:
    post:
      tags: [공동 편집]
      operationId: acceptCollaboration
      summary: 공동 편집 참여
      x-user-stories: [ARCH-11]
      description: |
        받은 초대를 수락하고 공동 편집자가 됩니다.
        수락한 뒤부터 편집 오퍼레이션을 호출할 수 있습니다.

        > **초대 거절 스토리가 없습니다.**
        > 유저 스토리에 수락(ARCH-11)만 있고 거절이 없어 거절 오퍼레이션을 만들지 않았습니다.
        > 거절이 필요하면 스토리를 먼저 추가해야 합니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      responses:
        '200':
          description: 참여 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Collaborator' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_COLLABORATOR` — 초대받은 적이 없음
            `COLLABORATION_CLOSED` — 수락 전에 공동 편집이 종료됨
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `ALREADY_COLLABORATOR` — 이미 수락한 초대
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/collaborators/me:
    delete:
      tags: [공동 편집]
      operationId: leaveCollaboration
      summary: 공동 편집 나가기
      x-user-stories: [ARCH-13]
      description: |
        참여 중인 아카이브에서 스스로 빠집니다.
        참여 상태가 `LEFT`가 되고 내 아카이브 목록에서 사라지며 편집 권한을 잃습니다.

        **소유자는 나갈 수 없습니다.** 소유자가 그만두려면 공동 편집을 종료하거나(ARCH-14)
        아카이브를 삭제해야 합니다(ARCH-09).

        내가 만든 부분은 아카이브에 남습니다. 내 기록으로 보관하려면
        나가기 전에 `POST /archives/{archiveId}/duplicate`로 복제하세요(ARCH-15).
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      responses:
        '200':
          description: 나가기 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 소유자는 나갈 수 없습니다
            `NOT_COLLABORATOR` — 참여 중이 아님
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/collaboration/close:
    post:
      tags: [공동 편집]
      operationId: closeCollaboration
      summary: 공동 편집 종료
      x-user-stories: [ARCH-14]
      description: |
        공동 편집을 끝냅니다. 소유자만 할 수 있습니다.

        종료 후 참여자는 **조회는 계속 할 수 있고 편집만 막힙니다**(ADR-017).
        내용까지 남기고 싶으면 `POST /archives/{archiveId}/duplicate`로 복제합니다(ARCH-15).
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      responses:
        '200':
          description: 종료 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ArchiveDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 소유자가 아님
            `COLLABORATION_CLOSED` — 이미 종료됨
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/collaboration/session:
    get:
      tags: [공동 편집]
      operationId: getCollaborationSession
      summary: 실시간 편집 접속 정보 조회
      x-user-stories: [ARCH-12]
      description: |
        실시간 공동 편집에 접속할 WebSocket 주소와 구독할 토픽을 받습니다.

        **실시간 편집 프로토콜은 `docs/realtime-editing.md`에 있습니다.**
        OpenAPI는 HTTP 요청·응답만 기술하므로 WebSocket 메시지는 그쪽에서 정의합니다.
        이 오퍼레이션은 접속에 필요한 정보만 넘겨줍니다.

        **편집 자체는 WebSocket으로 하지 않습니다.** 저장은 기존 HTTP 오퍼레이션으로 하고,
        WebSocket은 다른 참여자에게 변경을 알리는 통로입니다(ADR-051).

        **충돌 처리는 마지막 쓰기 우선입니다**(ADR-028).
        두 사람이 같은 사진을 동시에 옮기면 **나중에 도착한 변경이 이깁니다.**
        항목 단위 잠금을 두지 않으므로 편집이 막히는 일은 없고,
        드물게 남의 변경이 내 것으로 덮이는 대신 구현이 단순해집니다.

        이벤트 9종과 재접속·충돌 처리는 `docs/realtime-editing.md`에 정의되어 있습니다(ADR-051).
        **편집 저장은 WebSocket으로 하지 않습니다** — 기존 HTTP 오퍼레이션으로 저장하고
        WebSocket은 변경을 알리는 통로입니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      responses:
        '200':
          description: 접속 정보
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/CollaborationSession' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` · `NOT_COLLABORATOR` — 편집 권한 없음
            `COLLABORATION_CLOSED` — 종료된 공동 편집
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }


  # ------------------------------------------------------------------
  # 커뮤니티 — 게시물
  # ------------------------------------------------------------------

  /posts:
    get:
      tags: [커뮤니티]
      operationId: listPosts
      summary: 피드 조회
      x-user-stories: [COMM-01]
      description: |
        커뮤니티 전체 피드입니다. `sort`로 인기와 최신을 고릅니다.

        **인기는 좋아요 순입니다**(ADR-019). `posts.like_count` 컬럼으로 정렬합니다.

        **인기는 최근 30일 안에 올라온 게시물만 좋아요 순으로 줄 세웁니다**(ADR-044).
        30일이 지난 게시물은 좋아요가 많아도 인기 피드에 나오지 않습니다 — 최신 피드에서는 계속 보입니다.
      parameters:
        - name: sort
          in: query
          required: false
          schema: { $ref: '#/components/schemas/PostSortType' }
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PostSummary' }
                  meta: { $ref: '#/components/schemas/CursorMeta' }

    post:
      tags: [커뮤니티]
      operationId: createPost
      summary: 게시물 작성
      x-user-stories: [COMM-04]
      description: |
        코스·아카이브·둘 다 중에서 공유할 대상을 고르고 게시물을 올립니다.
        **최소 하나는 지정해야 합니다** — 둘 다 없으면 `INVALID_SHARE_TARGET`입니다.

        지역은 등록 시점에 **스냅샷으로 굳힙니다**(ADR-003).
        - 코스를 공유하면 일정의 여행지와 담긴 장소들의 지역을 **자동으로 합칩니다**
        - 코스 없이 아카이브만 공유하면 `regionCodes`를 **직접 보내야** 합니다

        스냅샷이라 나중에 원본 코스를 고쳐도 이 게시물의 지역 피드 노출은 바뀌지 않습니다.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PostCreateRequest' }
      responses:
        '200':
          description: 등록 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/PostDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_SHARE_TARGET` — 코스와 아카이브를 모두 지정하지 않음
            `REGION_REQUIRED` — 코스 없이 아카이브만 공유하면서 지역을 고르지 않음
            `INVALID_REQUEST` — 본문 길이 위반 등
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 남의 코스나 아카이브를 공유하려 함
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `TRIP_NOT_FOUND` · `ARCHIVE_NOT_FOUND` · `REGION_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /regions/{regionCode}/posts:
    get:
      tags: [커뮤니티]
      operationId: listRegionPosts
      summary: 지역별 피드 조회
      x-user-stories: [COMM-02]
      description: |
        해당 지역이 **코스에 포함되었거나 게시물에 직접 지정된** 게시물을 모두 반환합니다.
        한 여행이 여러 지역을 지나면 그 지역 피드마다 전부 노출됩니다.
        경유만 한 지역도 걸러내지 않습니다.

        판정은 `post_regions` 스냅샷으로 합니다 —
        원본 코스가 삭제된 게시물도 이 피드에서 사라지지 않습니다(ADR-003).
      parameters:
        - $ref: '#/components/parameters/RegionCode'
        - name: sort
          in: query
          required: false
          schema: { $ref: '#/components/schemas/PostSortType' }
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PostSummary' }
                  meta: { $ref: '#/components/schemas/CursorMeta' }
        '404':
          description: |
            `REGION_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /posts/{postId}:
    get:
      tags: [커뮤니티]
      operationId: getPost
      summary: 게시물 상세 조회
      x-user-stories: [COMM-03, COMM-17]
      description: |
        공유된 코스와 아카이브, 작성자 정보를 반환합니다.
        여기서 코스 가져오기(PLAN-12)와 아카이브 가져오기(ARCH-16)로 이어집니다.

        **원본이 삭제된 게시물도 `200`입니다**(COMM-17).
        게시물 자체는 남고 `sharedCourse`·`sharedArchive`만 `null`이 되며,
        `deletedShare`가 무엇이 사라졌는지 알려줍니다.

        댓글은 여기 담지 않습니다. 댓글이 많은 글에서 응답이 무거워지므로
        `GET /posts/{postId}/comments`로 나눠 받습니다.
      parameters:
        - $ref: '#/components/parameters/PostId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/PostDetail' }
                  meta: { type: 'null' }
        '404':
          description: |
            `POST_NOT_FOUND` — 게시물 자체가 삭제됨. 공유된 원본만 삭제된 경우는 `200`입니다
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    patch:
      tags: [커뮤니티]
      operationId: updatePost
      summary: 게시물 수정
      x-user-stories: [COMM-05]
      description: |
        게시물 본문을 고칩니다.

        **공유 대상과 지역은 바꿀 수 없습니다**(ADR-045). 본문만 고칩니다.
        공유 대상을 바꾸면 사실상 다른 게시물이 되어 이미 달린 댓글의 맥락이 깨지고,
        지역 스냅샷(ADR-003)도 흔들립니다. 잘못 올렸으면 지우고 다시 올립니다.
      parameters:
        - $ref: '#/components/parameters/PostId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content: { type: string, minLength: 1, maxLength: 2000 }
      responses:
        '200':
          description: 수정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/PostDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `POST_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [커뮤니티]
      operationId: deletePost
      summary: 게시물 삭제
      x-user-stories: [COMM-06]
      description: |
        게시물을 삭제합니다. 행을 지우지 않고 `status`를 `DELETED`로 바꿉니다(ADR-007).
        공유했던 코스와 아카이브는 **삭제되지 않습니다** — 공유를 거둘 뿐입니다.
      parameters:
        - $ref: '#/components/parameters/PostId'
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `POST_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /posts/{postId}/like:
    post:
      tags: [커뮤니티]
      operationId: likePost
      summary: 게시물 좋아요
      x-user-stories: [COMM-07]
      description: |
        좋아요를 누릅니다. 게시물의 좋아요 수가 늘고 내 좋아요 목록에 추가됩니다.
        중복은 `(post_id, user_id)` 복합키가 DB에서 막습니다(ADR-006).
      parameters:
        - $ref: '#/components/parameters/PostId'
      responses:
        '200':
          description: 좋아요 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/LikeResult' }
                  meta: { type: 'null' }
        '404':
          description: |
            `POST_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `ALREADY_LIKED` — 이미 좋아요한 게시물
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [커뮤니티]
      operationId: unlikePost
      summary: 게시물 좋아요 취소
      x-user-stories: [COMM-19]
      description: 좋아요를 취소합니다. 좋아요 수가 줄고 내 좋아요 목록에서 빠집니다.
      parameters:
        - $ref: '#/components/parameters/PostId'
      responses:
        '200':
          description: 취소 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/LikeResult' }
                  meta: { type: 'null' }
        '404':
          description: |
            `POST_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `NOT_LIKED` — 좋아요하지 않은 게시물
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /users/me/likes:
    get:
      tags: [커뮤니티]
      operationId: listMyLikedPosts
      summary: 좋아요 목록 조회
      x-user-stories: [COMM-08]
      description: |
        내가 좋아요한 게시물을 최근에 누른 순서로 반환합니다.
        번호 방식 페이지네이션입니다(ADR-011).

        좋아요한 뒤 삭제된 게시물은 목록에서 빠집니다.
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PostSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }

  /users/me/posts:
    get:
      tags: [커뮤니티]
      operationId: listMyPosts
      summary: 내 게시물 목록 조회
      x-user-stories: [COMM-09]
      description: |
        내가 작성한 게시물을 최신순으로 반환합니다.
        삭제한 게시물은 포함하지 않습니다.
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PostSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }

  # ------------------------------------------------------------------
  # 댓글 · 대댓글
  # ------------------------------------------------------------------

  /posts/{postId}/comments:
    get:
      tags: [댓글]
      operationId: listComments
      summary: 댓글 목록 조회
      x-user-stories: [COMM-03]
      description: |
        게시물의 댓글을 작성 순서대로 반환합니다.
        각 댓글에는 대댓글 개수와 **앞의 몇 개만** 함께 담기며,
        나머지는 `GET /comments/{commentId}/replies`로 더 받습니다.

        **COMM-03에서 갈라져 나온 오퍼레이션입니다.**
        스토리는 "댓글을 확인할 수 있다"로 게시물 상세에 포함되어 있지만,
        댓글이 수백 개인 글에서 상세 응답에 전부 담으면 화면이 열리지 않습니다.
      parameters:
        - $ref: '#/components/parameters/PostId'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/Comment' }
                  meta: { $ref: '#/components/schemas/CursorMeta' }
        '404':
          description: |
            `POST_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    post:
      tags: [댓글]
      operationId: createComment
      summary: 댓글 작성
      x-user-stories: [COMM-11]
      description: 게시물에 댓글을 답니다. 대댓글은 `POST /comments/{commentId}/replies`입니다.
      parameters:
        - $ref: '#/components/parameters/PostId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CommentWriteRequest' }
      responses:
        '200':
          description: 등록 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Comment' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `POST_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /comments/{commentId}:
    patch:
      tags: [댓글]
      operationId: updateComment
      summary: 댓글 수정
      x-user-stories: [COMM-12]
      parameters:
        - $ref: '#/components/parameters/CommentId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CommentWriteRequest' }
      responses:
        '200':
          description: 수정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Comment' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [댓글]
      operationId: deleteComment
      summary: 댓글 삭제
      x-user-stories: [COMM-13]
      description: |
        댓글을 삭제합니다. 행을 지우지 않고 `status`를 `DELETED`로 바꿉니다(ADR-007).

        **대댓글이 달린 댓글은 자리를 남깁니다.** 물리 삭제하면 남이 쓴 대댓글까지 사라집니다.
        내용은 `null`이 되고 앱은 "삭제된 댓글입니다"로 표시합니다.
      parameters:
        - $ref: '#/components/parameters/CommentId'
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /comments/{commentId}/replies:
    get:
      tags: [댓글]
      operationId: listReplies
      summary: 대댓글 목록 조회
      x-user-stories: [COMM-03]
      description: |
        댓글에 달린 대댓글을 작성 순서대로 반환합니다.
        댓글 목록에는 앞의 몇 개만 담기므로, "답글 더 보기"에서 이 오퍼레이션을 씁니다.
      parameters:
        - $ref: '#/components/parameters/CommentId'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/Reply' }
                  meta: { $ref: '#/components/schemas/CursorMeta' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    post:
      tags: [댓글]
      operationId: createReply
      summary: 대댓글 작성
      x-user-stories: [COMM-14]
      description: |
        댓글에 답글을 답니다. **깊이는 1단계까지입니다** —
        대댓글에 또 대댓글을 달 수는 없습니다.
      parameters:
        - $ref: '#/components/parameters/CommentId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CommentWriteRequest' }
      responses:
        '200':
          description: 등록 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Reply' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 본문 길이 위반, 또는 대댓글에 대댓글을 달려 함
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /replies/{replyId}:
    patch:
      tags: [댓글]
      operationId: updateReply
      summary: 대댓글 수정
      x-user-stories: [COMM-15]
      parameters:
        - $ref: '#/components/parameters/ReplyId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CommentWriteRequest' }
      responses:
        '200':
          description: 수정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Reply' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [댓글]
      operationId: deleteReply
      summary: 대댓글 삭제
      x-user-stories: [COMM-16]
      description: |
        대댓글을 삭제합니다. 아래에 달린 것이 없으므로 상태만 `DELETED`로 바꾸고
        목록에서 감춥니다. 댓글과 달리 자리를 남기지 않습니다.
      parameters:
        - $ref: '#/components/parameters/ReplyId'
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  # ------------------------------------------------------------------
  # 신고
  # ------------------------------------------------------------------

  /posts/{postId}/reports:
    post:
      tags: [신고]
      operationId: reportPost
      summary: 게시물 신고
      x-user-stories: [COMM-10]
      description: |
        부적절한 게시물을 운영자에게 알립니다.
        사유를 정해진 항목에서 고르고, 필요하면 설명을 덧붙입니다(ADR-046).
        접수만 되고 즉시 조치되지 않습니다 — 관리자가 검토해 유효로 판정해야
        작성자의 누적 신고 횟수에 반영됩니다(ADMIN-09).

        같은 대상을 반복 신고할 수 없습니다. `(신고자, 대상)` 유니크가 DB에서 막습니다(ADR-006).
      parameters:
        - $ref: '#/components/parameters/PostId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ReportRequest' }
      responses:
        '200':
          description: 신고 접수
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ReportResult' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 사유 누락, 또는 `ETC`인데 설명이 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `POST_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `ALREADY_REPORTED` — 이미 신고한 게시물
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /comments/{commentId}/reports:
    post:
      tags: [신고]
      operationId: reportComment
      summary: 댓글 신고
      x-user-stories: [COMM-18]
      description: 부적절한 댓글을 신고합니다. 대댓글은 `POST /replies/{replyId}/reports`입니다.
      parameters:
        - $ref: '#/components/parameters/CommentId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ReportRequest' }
      responses:
        '200':
          description: 신고 접수
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ReportResult' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `ALREADY_REPORTED`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /replies/{replyId}/reports:
    post:
      tags: [신고]
      operationId: reportReply
      summary: 대댓글 신고
      x-user-stories: [COMM-18]
      description: |
        부적절한 대댓글을 신고합니다.
        저장은 댓글 신고와 같은 테이블(`reports`)에 `target_type = COMMENT`로 들어갑니다.
        경로를 나눈 것은 관리자 삭제(ADMIN-07·ADMIN-08)가 댓글과 대댓글로 나뉘어 있어
        문서에서 짝을 맞추기 위해서입니다.
      parameters:
        - $ref: '#/components/parameters/ReplyId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ReportRequest' }
      responses:
        '200':
          description: 신고 접수
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ReportResult' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `ALREADY_REPORTED`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }


  # ------------------------------------------------------------------
  # 커뮤니티 — 작성자 프로필
  # ------------------------------------------------------------------

  /users/search:
    get:
      tags: [커뮤니티]
      operationId: searchUsers
      summary: 닉네임으로 사용자 검색
      x-user-stories: [ARCH-10]
      description: |
        공동 편집자로 초대할 사람을 닉네임으로 찾습니다(ADR-024).
        찾은 사용자의 `userId`를 `POST /archives/{archiveId}/collaborators`에 넘깁니다.

        **정확히 일치하는 닉네임만 반환합니다.** 부분 일치로 훑게 하면
        닉네임 목록을 긁어 모으는 통로가 됩니다.
        닉네임은 유일하므로(ADR-025) 결과는 **0개 아니면 1개**입니다.

        탈퇴하거나 차단된 회원은 결과에 나오지 않습니다.
      parameters:
        - name: nickname
          in: query
          required: true
          description: 정확히 일치하는 닉네임
          schema: { type: string, minLength: 1, maxLength: 20 }
      responses:
        '200':
          description: |
            검색 성공. 일치하는 사용자가 없으면 빈 배열입니다
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PublicProfile' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — `nickname`이 비어 있음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /users/{userId}:
    get:
      tags: [커뮤니티]
      operationId: getUserProfile
      summary: 작성자 프로필 조회
      x-user-stories: [COMM-20]
      description: |
        다른 사용자의 공개 프로필을 봅니다. 게시물에서 작성자를 눌렀을 때 들어옵니다.
        내 프로필은 `GET /users/me`이며, 그쪽에만 기록 현황과 온보딩 상태가 담깁니다.

        탈퇴하거나 차단된 회원은 `USER_NOT_FOUND`입니다 — 존재 여부를 알리지 않습니다.
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/PublicProfile' }
                  meta: { type: 'null' }
        '404':
          description: |
            `USER_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /users/{userId}/posts:
    get:
      tags: [커뮤니티]
      operationId: listUserPosts
      summary: 작성자의 게시물 목록 조회
      x-user-stories: [COMM-20]
      description: |
        그 사용자가 커뮤니티에 올린 게시물을 최신순으로 반환합니다.
        삭제된 게시물은 포함하지 않습니다.

        COMM-20의 결과가 "닉네임과 공개된 게시물 목록을 확인할 수 있다"인데,
        목록은 페이지를 나눠야 해서 프로필 조회와 오퍼레이션을 분리했습니다.
      parameters:
        - $ref: '#/components/parameters/UserId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/PostSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }
        '404':
          description: |
            `USER_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /archives/{archiveId}/collaborators/me/decline:
    post:
      tags: [공동 편집]
      operationId: declineCollaboration
      summary: 공동 편집 초대 거절
      x-user-stories: [ARCH-18]
      description: |
        받은 초대를 거절합니다. 초대가 사라지고 공동 편집자로 추가되지 않습니다.
        같은 아카이브에 다시 초대받는 것은 가능합니다.
      parameters:
        - $ref: '#/components/parameters/ArchiveId'
      responses:
        '200':
          description: 거절 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `NOT_COLLABORATOR` — 초대받은 적이 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `ARCHIVE_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `ALREADY_COLLABORATOR` — 이미 수락한 초대는 거절할 수 없습니다. 나가기(ARCH-13)를 쓰세요
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  # ------------------------------------------------------------------
  # 알림
  # ------------------------------------------------------------------

  /notifications:
    get:
      tags: [알림]
      operationId: listNotifications
      summary: 알림 목록 조회
      x-user-stories: [NOTI-01, NOTI-02, NOTI-05]
      description: |
        받은 알림을 최신순으로 반환합니다. 번호 방식 페이지네이션입니다(ADR-011).

        **공동 편집 초대 알림(NOTI-02)과 경고 알림(NOTI-05)은 별도 오퍼레이션이 아니라
        이 목록의 `type` 값입니다.** 알림 종류마다 조회를 나눌 이유가 없습니다.

        | `type` | 근거 | 눌렀을 때 |
        |---|---|---|
        | `COLLAB_INVITE` | NOTI-02 | 해당 아카이브 초대 화면 |
        | `CAPSULE_UNLOCK` | TC-04 | 해당 타임캡슐 |
        | `WARNING` | NOTI-05 | 경고 사유 안내 |

        **차단은 알림으로 오지 않습니다.** 차단된 사용자는 로그인 자체가 막혀
        알림 목록에 도달할 수 없어, `POST /auth/social/{provider}`의
        `403 USER_BLOCKED` 응답으로 안내합니다(AUTH-06).
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/Notification' }
                  meta: { $ref: '#/components/schemas/PageMeta' }

  /notifications/unread-count:
    get:
      tags: [알림]
      operationId: getUnreadNotificationCount
      summary: 안 읽은 알림 개수 확인
      x-user-stories: [NOTI-06]
      description: |
        앱 진입 시 배지에 띄울 숫자만 받습니다.
        알림 목록을 열지 않아도 새 알림이 있는지 알 수 있게 목록 조회와 분리했습니다.
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [unreadCount]
                    properties:
                      unreadCount: { type: integer, minimum: 0 }
                  meta: { type: 'null' }

  /notifications/read-all:
    post:
      tags: [알림]
      operationId: markAllNotificationsRead
      summary: 알림 모두 읽음 처리
      x-user-stories: [NOTI-04]
      description: 안 읽은 알림을 한 번에 모두 읽음으로 바꿉니다. 처리 후 개수는 `0`이 됩니다.
      responses:
        '200':
          description: 처리 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [unreadCount]
                    properties:
                      unreadCount: { type: integer, enum: [0] }
                  meta: { type: 'null' }

  /notifications/{notificationId}/read:
    patch:
      tags: [알림]
      operationId: markNotificationRead
      summary: 알림 읽음 처리
      x-user-stories: [NOTI-04]
      description: |
        알림 하나를 읽음으로 바꿉니다. 이미 읽은 알림에 다시 호출해도 `200`입니다 —
        같은 결과로 수렴하는 요청이라 `409`로 막지 않습니다.
      parameters:
        - $ref: '#/components/parameters/NotificationId'
      responses:
        '200':
          description: 처리 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Notification' }
                  meta: { type: 'null' }
        '403':
          description: |
            `NOT_RESOURCE_OWNER` — 남의 알림
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `NOTIFICATION_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /notifications/settings:
    get:
      tags: [알림]
      operationId: getNotificationSettings
      summary: 알림 설정 조회
      x-user-stories: [NOTI-03]
      description: |
        알림 종류별 수신 여부를 반환합니다.
        각 항목의 `editable`이 `false`면 사용자가 끌 수 없는 알림입니다.
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/NotificationSetting' }
                  meta: { type: 'null' }

    patch:
      tags: [알림]
      operationId: updateNotificationSettings
      summary: 알림 설정 변경
      x-user-stories: [NOTI-03]
      description: |
        받고 싶은 알림만 켭니다. 보내지 않은 항목은 그대로 둡니다.

        **경고 알림(`WARNING`)은 끌 수 없습니다.** 운영 통지라 수신 거부 대상이 아니며,
        끄면 경고를 못 보고 차단당합니다. 끄려고 시도하면 `INVALID_REQUEST`입니다.

        **설정은 알림 종류와 1:1입니다**(ADR-047). 시간대 같은 다른 축은 두지 않습니다.
        지금은 앱 내 알림만 있어 "야간 수신 안 함" 같은 설정이 실질적인 효과가 없습니다.
        나중에 푸시 알림을 붙일 때 다시 봅니다.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [settings]
              properties:
                settings:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required: [type, enabled]
                    properties:
                      type: { $ref: '#/components/schemas/NotificationType' }
                      enabled: { type: boolean }
      responses:
        '200':
          description: 저장 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/NotificationSetting' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 끌 수 없는 알림을 끄려 함
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  # ------------------------------------------------------------------
  # 관리자
  # ------------------------------------------------------------------

  /admin/auth/login:
    post:
      tags: [관리자]
      operationId: adminLogin
      summary: 관리자 로그인
      x-user-stories: [ADMIN-01]
      security: []
      description: |
        관리자 전용 계정으로 인증합니다.
        일반 회원은 소셜 로그인만 쓰므로(ADR-010) 관리자 계정은 별도 저장소(`admins`)를 씁니다.

        아이디와 비밀번호 중 무엇이 틀렸는지 구분해서 알리지 않습니다.

        인증은 **일반 회원과 같은 Bearer JWT를 쓰고 토큰의 역할 클레임으로 구분합니다**(ADR-036).
        관리자 전용 세션을 따로 두지 않으므로 인증 처리가 한 벌입니다.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [loginId, password]
              properties:
                loginId: { type: string }
                password: { type: string, format: password }
      responses:
        '200':
          description: 로그인 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/AdminSession' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 아이디나 비밀번호가 비어 있음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '401':
          description: |
            `ADMIN_CREDENTIALS_INVALID` — 아이디 또는 비밀번호 불일치
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/auth/logout:
    post:
      tags: [관리자]
      operationId: adminLogout
      summary: 관리자 로그아웃
      x-user-stories: [ADMIN-02]
      description: 관리자 세션을 끝냅니다. 발급된 토큰을 폐기합니다.
      responses:
        '200':
          description: 로그아웃 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '403':
          description: |
            `ADMIN_ONLY` — 관리자가 아님
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/dashboard:
    get:
      tags: [관리자]
      operationId: getAdminDashboard
      summary: 관리자 대시보드 조회
      x-user-stories: [ADMIN-03]
      description: |
        현황 숫자만 반환합니다. 회원 목록과 신고 목록은 각각 별도 오퍼레이션입니다.

        지표는 두 갈래입니다(ADR-035).
        **운영 지표**는 지금 무엇을 처리해야 하는지 알려주고,
        **콘텐츠 지표**는 서비스가 얼마나 쓰이는지 보여줍니다.

        `pendingReportCount`가 0보다 크면 관리자가 할 일이 있다는 뜻이라,
        화면에서 가장 눈에 띄게 두는 편이 좋습니다.
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/AdminDashboard' }
                  meta: { type: 'null' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/users:
    get:
      tags: [관리자]
      operationId: listAdminUsers
      summary: 회원 목록 조회
      x-user-stories: [ADMIN-12]
      description: |
        회원을 상태와 누적 신고 횟수와 함께 반환합니다.
        닉네임으로 검색하거나 상태로 걸러낼 수 있습니다.
        번호 방식 페이지네이션입니다(ADR-011).
      parameters:
        - name: keyword
          in: query
          required: false
          description: 닉네임 부분 일치
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { $ref: '#/components/schemas/UserStatus' }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/AdminUserSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/users/{userId}:
    get:
      tags: [관리자]
      operationId: getAdminUserDetail
      summary: 회원 상세 조회
      x-user-stories: [ADMIN-13]
      description: |
        제재 전에 확인할 회원 이력입니다.
        가입 정보, 누적 유효 신고 횟수, 지금까지의 경고·차단 이력이 담깁니다.

        **차단 해제(ADMIN-11) 전에 왜 차단됐는지 확인하는 화면이기도 합니다.**
        탈퇴한 회원도 조회됩니다 — 운영 이력은 남겨야 합니다.
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/AdminUserDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `USER_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/users/{userId}/warnings:
    post:
      tags: [관리자]
      operationId: warnUser
      summary: 회원 경고
      x-user-stories: [ADMIN-04]
      description: |
        누적 기준과 별개로 관리자가 즉시 경고를 내립니다.
        `sanctions`에 `source = MANUAL`로 기록되고 회원에게 알림이 갑니다(NOTI-05).

        누적 유효 신고로 자동 발생하는 경고는 이 오퍼레이션이 아니라
        신고 판정(ADMIN-09)의 결과로 처리됩니다(ADMIN-10).
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SanctionRequest' }
      responses:
        '200':
          description: 경고 처리 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Sanction' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 사유 누락
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `USER_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/users/{userId}/blocks:
    post:
      tags: [관리자]
      operationId: blockUser
      summary: 회원 차단
      x-user-stories: [ADMIN-05]
      description: |
        회원의 서비스 이용을 막습니다. `users.status`가 `BLOCKED`이 되고
        발급된 토큰이 폐기됩니다.

        차단된 회원은 로그인 시 `403 USER_BLOCKED`와 함께 사유를 안내받습니다(AUTH-06).
        알림으로는 전달할 수 없습니다 — 앱에 들어올 수 없으니까요.

        **차단에 기간을 두지 않습니다**(ADR-033). 관리자가 직접 풀어줄 때까지 유지되며,
        해제는 `DELETE /admin/users/{userId}/blocks`입니다(ADMIN-11).
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SanctionRequest' }
      responses:
        '200':
          description: 차단 처리 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/Sanction' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 사유 누락
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `USER_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `DUPLICATE_REQUEST` — 이미 차단된 회원
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    delete:
      tags: [관리자]
      operationId: unblockUser
      summary: 회원 차단 해제
      x-user-stories: [ADMIN-11]
      description: |
        차단을 풀어 서비스를 다시 이용할 수 있게 합니다.
        해제 사유도 `sanctions`에 이력으로 남깁니다 — 누가 왜 풀었는지 추적할 수 있어야 합니다.

        **차단되지 않은 회원에게 호출해도 `200`입니다.** 결과가 같은 곳으로 수렴하는 요청이라
        오류로 막지 않습니다.
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SanctionRequest' }
      responses:
        '200':
          description: 해제 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/AdminUserDetail' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — 사유 누락
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `USER_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/reports:
    get:
      tags: [관리자]
      operationId: listAdminReports
      summary: 신고 목록 조회
      x-user-stories: [ADMIN-14]
      description: |
        접수된 신고를 반환합니다. 기본은 미처리(`PENDING`) 먼저, 접수 순서대로입니다.
        `status`로 처리된 것만 따로 볼 수 있습니다.
      parameters:
        - name: status
          in: query
          required: false
          schema: { $ref: '#/components/schemas/ReportStatus' }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/AdminReportSummary' }
                  meta: { $ref: '#/components/schemas/PageMeta' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/reports/{reportId}:
    get:
      tags: [관리자]
      operationId: getAdminReportDetail
      summary: 신고 상세 조회
      x-user-stories: [ADMIN-15]
      description: |
        신고 사유와 **신고당한 게시물·댓글의 실제 내용**, 작성자를 함께 반환합니다.
        내용을 보지 않고는 유효 여부를 판정할 수 없어 판정(ADMIN-09) 직전에 거치는 화면입니다.

        신고 대상이 이미 삭제되었으면 `targetContent`가 `null`이고
        `targetDeleted`가 `true`입니다 — 그래도 판정은 할 수 있습니다.
      parameters:
        - $ref: '#/components/parameters/ReportId'
      responses:
        '200':
          description: 조회 성공
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/AdminReportDetail' }
                  meta: { type: 'null' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `REPORT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

    patch:
      tags: [관리자]
      operationId: reviewReport
      summary: 신고 검토 및 유효 판정
      x-user-stories: [ADMIN-09, ADMIN-10]
      description: |
        신고를 유효(`VALID`) 또는 반려(`REJECTED`)로 판정합니다.

        **유효로 판정하면 자동 제재가 이어서 일어납니다(ADMIN-10).**
        신고당한 회원의 누적 유효 신고 횟수가 1 늘고, 그 값이 기준에 닿으면
        시스템이 제재를 겁니다 — **1회면 경고, 3회면 차단.**
        관리자가 따로 누르지 않습니다. 응답의 `appliedSanction`으로 무엇이 일어났는지 확인합니다.

        ADMIN-10은 이 오퍼레이션의 결과라 별도 엔드포인트가 없습니다.
      parameters:
        - $ref: '#/components/parameters/ReportId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [VALID, REJECTED] }
                note: { type: [string, 'null'], maxLength: 200, description: 판정 메모 }
      responses:
        '200':
          description: 판정 완료
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: '#/components/schemas/ReportReviewResult' }
                  meta: { type: 'null' }
        '400':
          description: |
            `INVALID_REQUEST` — `status`가 `VALID`·`REJECTED`가 아님
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `REPORT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: |
            `REPORT_ALREADY_REVIEWED` — 이미 판정된 신고
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/posts/{postId}:
    delete:
      tags: [관리자]
      operationId: adminDeletePost
      summary: 게시물 삭제
      x-user-stories: [ADMIN-06]
      description: |
        정책에 반하는 게시물을 운영자 권한으로 내립니다. 사유를 반드시 남깁니다.
        작성자 본인의 삭제(COMM-06)와 달리 **누가 왜 지웠는지가 기록됩니다.**
      parameters:
        - $ref: '#/components/parameters/PostId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SanctionRequest' }
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '400':
          description: |
            `INVALID_REQUEST` — 사유 누락
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `POST_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/comments/{commentId}:
    delete:
      tags: [관리자]
      operationId: adminDeleteComment
      summary: 댓글 삭제
      x-user-stories: [ADMIN-07]
      description: |
        정책에 반하는 댓글을 내립니다. 대댓글이 달려 있으면 자리는 남깁니다(ADR-007).
      parameters:
        - $ref: '#/components/parameters/CommentId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SanctionRequest' }
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '400':
          description: |
            `INVALID_REQUEST` — 사유 누락
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

  /admin/replies/{replyId}:
    delete:
      tags: [관리자]
      operationId: adminDeleteReply
      summary: 대댓글 삭제
      x-user-stories: [ADMIN-08]
      description: 정책에 반하는 대댓글을 내립니다. 저장은 댓글과 같은 테이블입니다.
      parameters:
        - $ref: '#/components/parameters/ReplyId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SanctionRequest' }
      responses:
        '200':
          description: 삭제 완료
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmptySuccess' }
        '400':
          description: |
            `INVALID_REQUEST` — 사유 누락
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: |
            `ADMIN_ONLY`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404':
          description: |
            `COMMENT_NOT_FOUND`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }


components:

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        서버가 발급한 액세스 토큰. 만료 30분.
        관리자도 같은 스킴을 쓰되 토큰의 역할 클레임으로 구분합니다(ADR-036).

  parameters:

    Provider:
      name: provider
      in: path
      required: true
      description: 소셜 로그인 제공자
      schema:
        type: string
        enum: [google, kakao, naver]

    Page:
      name: page
      in: query
      required: false
      description: 1부터 시작하는 페이지 번호
      schema: { type: integer, minimum: 1, default: 1 }

    Cursor:
      name: cursor
      in: query
      required: false
      description: 이전 응답의 `meta.nextCursor`. 첫 페이지에서는 생략합니다
      schema: { type: string }

    Size:
      name: size
      in: query
      required: false
      description: 한 번에 받을 개수
      schema: { type: integer, minimum: 1, maximum: 50, default: 20 }

    TripId:
      name: tripId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    TripPlaceId:
      name: tripPlaceId
      in: path
      required: true
      description: 일정에 담긴 장소 한 건의 식별자. 관광지 자체의 `placeId`와 다릅니다
      schema: { type: integer, format: int64 }

    PlaceId:
      name: placeId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    RegionCode:
      name: regionCode
      in: path
      required: true
      description: 콘텐츠랩 areaCode 또는 sigunguCode(ADR-004)
      schema: { type: string }

    VisitId:
      name: visitId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    CapsuleId:
      name: capsuleId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    ArchiveId:
      name: archiveId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    ArchiveItemId:
      name: itemId
      in: path
      required: true
      description: 아카이브에 배치된 항목 한 건. 사진 자체의 `mediaId`와 다릅니다
      schema: { type: integer, format: int64 }

    PostId:
      name: postId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    CommentId:
      name: commentId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    UserId:
      name: userId
      in: path
      required: true
      description: '`me`는 내 계정을 가리키는 예약어라 사용자 식별자로 쓸 수 없습니다'
      schema: { type: integer, format: int64 }

    NotificationId:
      name: notificationId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    ReportId:
      name: reportId
      in: path
      required: true
      schema: { type: integer, format: int64 }

    ReplyId:
      name: replyId
      in: path
      required: true
      description: 대댓글도 댓글과 같은 저장소를 쓰지만 경로를 나눠 스토리를 구분합니다
      schema: { type: integer, format: int64 }

  schemas:

    # --- 공통 봉투 ---

    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              description: '`docs/error-codes.md`에 정의된 코드'
              examples: [TRIP_NOT_FOUND]
            message:
              type: string
              description: 사용자에게 그대로 보여줄 수 있는 한국어 문장
            details:
              type: [object, 'null']
              description: 코드별 부가 정보. 검증 실패일 때만 형태가 고정됩니다
              properties:
                fields:
                  type: array
                  items:
                    type: object
                    properties:
                      field: { type: string }
                      reason: { type: string }

    EmptySuccess:
      type: object
      description: 반환할 본문이 없는 성공 응답
      properties:
        data: { type: 'null' }
        meta: { type: 'null' }

    PageMeta:
      type: object
      description: 번호 방식 목록의 페이지 정보
      required: [page, size, totalElements, totalPages]
      properties:
        page: { type: integer, examples: [1] }
        size: { type: integer, examples: [20] }
        totalElements: { type: integer, examples: [137] }
        totalPages: { type: integer, examples: [7] }

    CursorMeta:
      type: object
      description: 커서 방식 목록의 이어보기 정보
      required: [hasNext]
      properties:
        nextCursor:
          type: [string, 'null']
          description: 다음 요청의 `cursor`로 그대로 넣습니다. 클라이언트가 해석하지 않습니다
        hasNext: { type: boolean }

    # --- 인증 ---

    TokenPair:
      type: object
      required: [accessToken, refreshToken]
      properties:
        accessToken:
          type: string
          description: 만료 30분
        refreshToken:
          type: string
          description: 만료 7일. 재발급 시 함께 교체됩니다

    AuthResult:
      allOf:
        - $ref: '#/components/schemas/TokenPair'
        - type: object
          required: [isNewUser, onboardingCompleted]
          properties:
            isNewUser:
              type: boolean
              description: 이번 요청으로 회원가입이 일어났으면 `true`(AUTH-01), 기존 회원이면 `false`(AUTH-02)
            onboardingCompleted:
              type: boolean
              description: '`false`이면 앱이 초기 설정 화면으로 보냅니다'

    # --- 사용자 ---

    MyProfile:
      type: object
      required: [userId, nickname, onboardingCompleted]
      properties:
        userId: { type: integer, format: int64 }
        nickname:
          type: string
          maxLength: 20
        profileImageUrl:
          type: [string, 'null']
          format: uri
        onboardingCompleted: { type: boolean }
        stats:
          $ref: '#/components/schemas/MyStats'

    MyStats:
      type: object
      description: 마이페이지에 보여줄 기록 현황(ADR-034). 네 갈래로 묶여 있습니다
      required: [archiveCount, tripCount, visitedRegionCount, gardenUnlockedCount, gardenTotalCount, postCount, receivedLikeCount, sealedCapsuleCount, unlockableCapsuleCount]
      properties:
        archiveCount:
          type: integer
          description: '**기록** — 내가 만든 아카이브 수'
        tripCount:
          type: integer
          description: '**기록** — 내가 만든 여행 일정 수'
        visitedRegionCount:
          type: integer
          description: '**기록** — 방문 인증을 마친 지역 수'
        gardenUnlockedCount:
          type: integer
          description: '**정원** — 해금한 식물·오브젝트 수. `GET /garden`의 `unlockedCount`와 같은 값'
        gardenTotalCount:
          type: integer
          description: '**정원** — 도감 전체 수. "12 / 40"처럼 진도를 보여주기 위한 값'
        postCount:
          type: integer
          description: '**커뮤니티** — 내가 올린 게시물 수. 삭제한 것은 제외'
        receivedLikeCount:
          type: integer
          description: '**커뮤니티** — 내 게시물이 받은 좋아요 총합'
        sealedCapsuleCount:
          type: integer
          description: '**타임캡슐** — 아직 봉인된 캡슐 수'
        unlockableCapsuleCount:
          type: integer
          description: |
            **타임캡슐** — 조건이 충족돼 지금 열 수 있는 캡슐 수.
            0보다 크면 앱이 마이페이지에서 바로 안내할 수 있습니다

    ProfileUpdateRequest:
      type: object
      description: 보내지 않은 필드는 변경하지 않습니다
      minProperties: 1
      properties:
        nickname:
          type: string
          minLength: 1
          maxLength: 20
          pattern: '^[가-힣a-zA-Z0-9]{1,20}$'
          description: |
            한글·영문·숫자만 허용합니다. 공백과 특수문자는 쓸 수 없습니다(ADR-053).
            중복될 수 없습니다(ADR-025). 이미 쓰이는 닉네임이면 `409 NICKNAME_DUPLICATED`입니다.
        profileImageUrl:
          type: [string, 'null']
          format: uri
          description: '`null`을 보내면 기본 이미지로 되돌립니다'

    # --- 여행 일정(코스) ---

    TripStatus:
      type: string
      enum: [UPCOMING, ONGOING, PAST]
      description: 오늘 날짜와 여행 기간을 비교해 서버가 계산합니다

    TripCreateRequest:
      type: object
      required: [title, startDate, endDate, regionCodes]
      properties:
        title: { type: string, minLength: 1, maxLength: 60 }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        regionCodes:
          type: array
          minItems: 1
          description: 여행지. 복수 선택 가능합니다(PLAN-01)
          items: { type: string }

    TripUpdateRequest:
      type: object
      description: 보내지 않은 필드는 변경하지 않습니다
      minProperties: 1
      properties:
        title: { type: string, minLength: 1, maxLength: 60 }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        regionCodes:
          type: array
          minItems: 1
          items: { type: string }

    TripSummary:
      type: object
      required: [tripId, title, startDate, endDate, status, regions, placeCount]
      properties:
        tripId: { type: integer, format: int64 }
        title: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        status: { $ref: '#/components/schemas/TripStatus' }
        regions:
          type: array
          items: { $ref: '#/components/schemas/RegionSummary' }
        placeCount:
          type: integer
          description: 담긴 장소 수
        thumbnailUrl:
          type: [string, 'null']
          format: uri
          description: 담긴 장소 중 첫 번째의 대표 이미지
        linkedArchiveId:
          type: [integer, 'null']
          format: int64
          description: 연결된 아카이브. 없으면 `null`(ADR-001)

    TripDetail:
      allOf:
        - $ref: '#/components/schemas/TripSummary'
        - type: object
          required: [days]
          properties:
            days:
              type: array
              description: 여행 기간만큼의 일자가 장소가 없어도 모두 들어갑니다
              items: { $ref: '#/components/schemas/TripDay' }
            originTripId:
              type: [integer, 'null']
              format: int64
              description: 남의 코스를 가져와 만든 일정이면 원본 식별자(ADR-005)

    TripDay:
      type: object
      required: [dayNumber, date, places]
      properties:
        dayNumber: { type: integer, minimum: 1, description: '여행 1일차 = 1' }
        date: { type: string, format: date }
        places:
          type: array
          description: '`sortOrder` 오름차순으로 정렬되어 있습니다'
          items: { $ref: '#/components/schemas/TripPlace' }

    TripPlace:
      type: object
      required: [tripPlaceId, place, dayNumber, sortOrder]
      properties:
        tripPlaceId:
          type: integer
          format: int64
          description: 일정에 담긴 이 한 건의 식별자. 같은 장소를 다른 날 또 담으면 다른 값입니다
        place: { $ref: '#/components/schemas/PlaceSummary' }
        dayNumber: { type: integer, minimum: 1 }
        sortOrder: { type: integer, minimum: 1, description: 그 날의 방문 순서 }
        memo: { type: [string, 'null'] }

    TripPlaceCreateRequest:
      type: object
      required: [placeId, dayNumber]
      properties:
        placeId: { type: integer, format: int64 }
        dayNumber: { type: integer, minimum: 1 }
        sortOrder:
          type: [integer, 'null']
          minimum: 1
          description: 생략하면 해당 일자의 맨 뒤에 붙습니다
        memo: { type: [string, 'null'], maxLength: 500 }

    TripPlaceUpdateRequest:
      type: object
      description: 보내지 않은 필드는 변경하지 않습니다
      minProperties: 1
      properties:
        dayNumber: { type: integer, minimum: 1 }
        sortOrder: { type: integer, minimum: 1 }
        memo: { type: [string, 'null'], maxLength: 500 }

    TripPlaceOrderRequest:
      type: object
      required: [items]
      description: 일정에 담긴 **모든** 장소를 보내야 합니다. 부분 전송은 거부됩니다
      properties:
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [tripPlaceId, dayNumber, sortOrder]
            properties:
              tripPlaceId: { type: integer, format: int64 }
              dayNumber: { type: integer, minimum: 1 }
              sortOrder: { type: integer, minimum: 1 }

    AutoArrangeRequest:
      type: object
      description: 본문 없이 호출하면 일정에 이미 담긴 장소만 재배치합니다
      properties:
        additionalPlaceIds:
          type: array
          description: |
            아직 일정에 없지만 이번 배치에 함께 넣어보고 싶은 장소들.
            **제안에만 반영되고 저장되지 않습니다**(ADR-031)
          items: { type: integer, format: int64 }

    AutoArrangeResult:
      type: object
      description: |
        제안된 배치입니다. **저장되지 않았습니다.**
        `items`를 그대로 `PUT /trips/{tripId}/places/order`에 보내면 적용됩니다
      required: [items, currentDistanceMeters, proposedDistanceMeters]
      properties:
        items:
          type: array
          description: '`TripPlaceOrderRequest.items`와 같은 모양입니다'
          items:
            type: object
            required: [tripPlaceId, dayNumber, sortOrder]
            properties:
              tripPlaceId: { type: integer, format: int64 }
              dayNumber: { type: integer, minimum: 1 }
              sortOrder: { type: integer, minimum: 1 }
              place: { $ref: '#/components/schemas/PlaceSummary' }
        currentDistanceMeters:
          type: integer
          description: 지금 순서대로 다녔을 때의 이동 거리 합
        proposedDistanceMeters:
          type: integer
          description: 제안한 순서로 다녔을 때의 이동 거리 합

    TripRoute:
      type: object
      required: [tripId, days]
      properties:
        tripId: { type: integer, format: int64 }
        days:
          type: array
          items:
            type: object
            required: [dayNumber, points]
            properties:
              dayNumber: { type: integer, minimum: 1 }
              points:
                type: array
                description: 방문 순서대로 정렬된 좌표
                items:
                  type: object
                  required: [sortOrder, lat, lng, title]
                  properties:
                    sortOrder: { type: integer, minimum: 1 }
                    tripPlaceId: { type: integer, format: int64 }
                    lat: { type: number, format: double }
                    lng: { type: number, format: double }
                    title: { type: string }
              totalDistanceMeters:
                type: [integer, 'null']
                description: |
                  그 날 이동 거리의 합. **직선 거리 기준입니다**(ADR-043).
                  실제 도로를 따라간 거리가 아니므로 실제보다 짧게 나옵니다
        boundingBox:
          type: object
          description: 지도를 이 범위에 맞추면 동선 전체가 들어옵니다
          required: [minLat, minLng, maxLat, maxLng]
          properties:
            minLat: { type: number, format: double }
            minLng: { type: number, format: double }
            maxLat: { type: number, format: double }
            maxLng: { type: number, format: double }

    # --- 지역 · 장소 ---

    RegionSummary:
      type: object
      required: [code, name, level]
      properties:
        code:
          type: string
          description: 콘텐츠랩 areaCode / sigunguCode 원본(ADR-004)
        name: { type: string }
        level: { type: string, enum: [SIDO, SIGUNGU] }

    PlaceSummary:
      type: object
      required: [placeId, title, lat, lng, region]
      properties:
        placeId: { type: integer, format: int64 }
        contentId:
          type: string
          description: 콘텐츠랩 contentid
        contentTypeId:
          type: [string, 'null']
          description: 관광지·음식점·숙박 등 분류 코드
        title: { type: string }
        thumbnailUrl: { type: [string, 'null'], format: uri }
        lat: { type: number, format: double }
        lng: { type: number, format: double }
        region: { $ref: '#/components/schemas/RegionSummary' }

    PlaceDetail:
      allOf:
        - $ref: '#/components/schemas/PlaceSummary'
        - type: object
          properties:
            addr: { type: [string, 'null'] }
            tel: { type: [string, 'null'] }
            overview:
              type: [string, 'null']
              description: 콘텐츠랩 소개글
            useTime:
              type: [string, 'null']
              description: |
                이용 시간. 콘텐츠랩 원문 그대로입니다(ADR-049).
                "09:00~18:00", "상시개방"처럼 형식이 제각각이라 그대로 보여줍니다
            restDate:
              type: [string, 'null']
              description: 휴무일. 콘텐츠랩 원문 그대로 ("연중무휴", "매주 월요일" 등)
            imageUrls:
              type: array
              description: 관광 사진 정보에서 받은 공공저작물 이미지
              items: { type: string, format: uri }

    # --- 지도 · 방문 인증 ---

    RegionVisitRequest:
      type: object
      required: [lat, lng, accuracyMeters]
      properties:
        lat: { type: number, format: double }
        lng: { type: number, format: double }
        accuracyMeters:
          type: number
          description: |
            단말이 좌표와 함께 알려주는 정확도(iOS `horizontalAccuracy`, Android `getAccuracy()`).
            100m를 넘으면 거절합니다(ADR-016)
        regionCode:
          type: [string, 'null']
          description: |
            앱이 판단한 현재 지역. 보내면 서버 판정 결과와 대조하고,
            다르면 `LOCATION_MISMATCH`로 거절합니다. 생략하면 서버 판정을 그대로 씁니다

    RewardStatus:
      type: string
      enum: [UNLOCKED, GROWN, COOLDOWN, NONE]
      description: |
        `UNLOCKED` 처음 방문해 오브젝트 해금 · `GROWN` 기존 오브젝트가 성장 ·
        `COOLDOWN` 7일 안에 재인증이라 변화 없음 · `NONE` 그 지역에 지정된 오브젝트가 없음

    RegionVisitResult:
      type: object
      required: [visitId, region, verifiedAt, isFirstVisit, rewardStatus]
      properties:
        visitId: { type: integer, format: int64 }
        region: { $ref: '#/components/schemas/RegionSummary' }
        verifiedAt: { type: string, format: date-time }
        isFirstVisit: { type: boolean }
        rewardStatus: { $ref: '#/components/schemas/RewardStatus' }
        reward:
          oneOf:
            - $ref: '#/components/schemas/VisitReward'
            - type: 'null'
          description: 보상 상세. `rewardStatus`가 `NONE`이면 `null`

    RegionVisitStatus:
      type: object
      required: [region, visited]
      properties:
        region: { $ref: '#/components/schemas/RegionSummary' }
        visited: { type: boolean }
        visitCount: { type: integer, description: 인증 횟수 }
        lastVisitedAt: { type: [string, 'null'], format: date-time }

    RegionDetail:
      allOf:
        - $ref: '#/components/schemas/RegionSummary'
        - type: object
          properties:
            centerLat: { type: number, format: double }
            centerLng: { type: number, format: double }
            visited: { type: boolean }
            visitCount: { type: integer }
            availableGardenObjects:
              type: array
              description: 이 지역에서 얻을 수 있는 식물·오브젝트
              items: { $ref: '#/components/schemas/GardenObject' }

    # --- 정원 ---

    GardenObjectType:
      type: string
      enum: [PLANT, OBJECT]

    GardenObject:
      type: object
      description: 도감 정보. 사용자가 가졌는지와 무관한 마스터 데이터입니다
      required: [gardenObjectId, name, type, maxStage]
      properties:
        gardenObjectId: { type: integer, format: int64 }
        name: { type: string }
        type: { $ref: '#/components/schemas/GardenObjectType' }
        imageUrl: { type: [string, 'null'], format: uri }
        maxStage: { type: integer, minimum: 1, description: 더 자랄 수 없는 단계 }
        region:
          oneOf:
            - $ref: '#/components/schemas/RegionSummary'
            - type: 'null'
          description: 지역 전용이면 해당 지역, 공통이면 `null`

    Garden:
      type: object
      required: [objects, unlockedCount, totalCount]
      properties:
        objects:
          type: array
          items: { $ref: '#/components/schemas/UserGardenObject' }
        unlockedCount: { type: integer, description: 해금한 오브젝트 수 }
        totalCount: { type: integer, description: 도감 전체 오브젝트 수 }

    UserGardenObject:
      type: object
      required: [userGardenObjectId, gardenObject, stage]
      properties:
        userGardenObjectId: { type: integer, format: int64 }
        gardenObject: { $ref: '#/components/schemas/GardenObject' }
        stage: { type: integer, minimum: 1, description: 현재 성장 단계 }
        positionX: { type: [integer, 'null'], description: 정원 안 배치 좌표 }
        positionY: { type: [integer, 'null'] }
        unlockedAt: { type: string, format: date-time }
        lastGrownAt: { type: [string, 'null'], format: date-time }

    VisitReward:
      type: object
      required: [visitId, region, rewardStatus]
      properties:
        visitId: { type: integer, format: int64 }
        region: { $ref: '#/components/schemas/RegionSummary' }
        rewardStatus: { $ref: '#/components/schemas/RewardStatus' }
        gardenObject:
          oneOf:
            - $ref: '#/components/schemas/GardenObject'
            - type: 'null'
        previousStage:
          type: [integer, 'null']
          description: 성장 전 단계. 해금이면 `null`
        currentStage: { type: [integer, 'null'] }
        nextAvailableAt:
          type: [string, 'null']
          format: date-time
          description: '`rewardStatus`가 `COOLDOWN`일 때 다음 성장 가능 시각(ADR-018)'

    # --- 미디어 ---

    MediaType:
      type: string
      enum: [IMAGE, VIDEO]

    MediaStatus:
      type: string
      enum: [PENDING, READY]
      description: |
        `PENDING` URL만 발급된 상태 · `READY` 업로드가 끝나 아카이브·타임캡슐에 담을 수 있는 상태.
        **`PENDING`인 `mediaId`는 다른 도메인에서 쓸 수 없습니다**(ADR-023)

    MediaUploadRequest:
      type: object
      required: [fileName, contentType, sizeBytes]
      properties:
        fileName: { type: string, maxLength: 255 }
        contentType:
          type: string
          description: 'image/jpeg · image/png · image/heic · image/webp · video/mp4 · video/quicktime'
        sizeBytes: { type: integer, minimum: 1 }
        width:
          type: [integer, 'null']
          description: |
            **영상이면 필수입니다**(ADR-052). 사진은 서버가 EXIF에서 읽으므로 보내지 않아도 됩니다.
            영상에서 빠지면 `INVALID_REQUEST`입니다
        height: { type: [integer, 'null'], description: 영상이면 필수 }
        durationMs:
          type: [integer, 'null']
          description: |
            영상이면 필수. 재생 시간 표시에 씁니다.
            **앱이 보내는 값이라 서버가 검증하지 못합니다** — 3분 제한은
            `sizeBytes`(200MB)로 실질적으로 막습니다

    MediaUploadTicket:
      type: object
      required: [mediaId, uploadUrl, expiresAt]
      properties:
        mediaId:
          type: integer
          format: int64
          description: 업로드를 마친 뒤 `POST /media/complete`에 그대로 보냅니다
        uploadUrl:
          type: string
          format: uri
          description: 이 주소에 파일을 `PUT`으로 올립니다. 이 API가 아니라 스토리지로 직접 보냅니다
        expiresAt: { type: string, format: date-time }

    Media:
      type: object
      required: [mediaId, type, status, url]
      properties:
        mediaId: { type: integer, format: int64 }
        status: { $ref: '#/components/schemas/MediaStatus' }
        type: { $ref: '#/components/schemas/MediaType' }
        url: { type: string, format: uri }
        thumbnailUrl:
          type: [string, 'null']
          format: uri
          description: |
            사진은 리사이즈한 이미지, **영상은 항상 `null`입니다**(ADR-052).
            영상 미리보기는 앱이 `url`에서 첫 프레임을 직접 렌더합니다.
            나중에 서버가 썸네일을 만들게 되면 이 필드를 채우면 되고 계약은 바뀌지 않습니다
        width: { type: [integer, 'null'] }
        height: { type: [integer, 'null'] }
        durationMs:
          type: [integer, 'null']
          description: 영상만 채워집니다
        sizeBytes: { type: integer }
        takenAt:
          type: [string, 'null']
          format: date-time
          description: EXIF 촬영 시각. 없으면 `null`
        lat:
          type: [number, 'null']
          format: double
          description: EXIF 촬영 좌표. 없으면 `null`
        lng: { type: [number, 'null'], format: double }
        createdAt: { type: string, format: date-time }

    # --- 타임캡슐 ---

    TimeCapsuleStatus:
      type: string
      enum: [SEALED, UNLOCKABLE, OPENED]
      description: |
        `SEALED` 조건 미충족 · `UNLOCKABLE` 조건 충족, 아직 열지 않음 · `OPENED` 열어봄

    UnlockType:
      type: string
      enum: [DATE, LOCATION]

    UnlockCondition:
      type: object
      required: [type, satisfied]
      properties:
        type: { $ref: '#/components/schemas/UnlockType' }
        satisfied:
          type: boolean
          description: 지금 조건이 충족된 상태인지
        unlockDate:
          type: [string, 'null']
          format: date
          description: '`type`이 `DATE`일 때만 채워집니다'
        lat: { type: [number, 'null'], format: double }
        lng: { type: [number, 'null'], format: double }
        radiusMeters:
          type: [integer, 'null']
          description: '`type`이 `LOCATION`일 때만 채워집니다'
        placeName:
          type: [string, 'null']
          description: 사용자가 알아볼 수 있게 저장해 둔 장소 이름

    TimeCapsuleCreateRequest:
      type: object
      required: [title, content, unlockType]
      description: 해제 조건은 날짜와 위치 중 **하나만** 지정합니다
      properties:
        title: { type: string, minLength: 1, maxLength: 60 }
        content: { type: string, minLength: 1, maxLength: 5000 }
        unlockType: { $ref: '#/components/schemas/UnlockType' }
        unlockDate:
          type: [string, 'null']
          format: date
          description: '`unlockType`이 `DATE`일 때 필수. 오늘보다 뒤여야 합니다'
        unlockLat: { type: [number, 'null'], format: double }
        unlockLng: { type: [number, 'null'], format: double }
        unlockRadiusMeters:
          type: [integer, 'null']
          minimum: 50
          description: '`unlockType`이 `LOCATION`일 때 필수'
        placeName: { type: [string, 'null'], maxLength: 60 }
        mediaIds:
          type: array
          description: '업로드를 마쳐 `READY` 상태가 된 사진·영상의 식별자(ADR-023)'
          items: { type: integer, format: int64 }

    TimeCapsuleSummary:
      type: object
      description: 목록용. **내용을 포함하지 않습니다**
      required: [capsuleId, title, status, unlockType, createdAt]
      properties:
        capsuleId: { type: integer, format: int64 }
        title: { type: string }
        status: { $ref: '#/components/schemas/TimeCapsuleStatus' }
        unlockType: { $ref: '#/components/schemas/UnlockType' }
        thumbnailUrl:
          type: [string, 'null']
          format: uri
          description: 열어본 캡슐만 채워집니다. 봉인 상태에서는 `null`
        createdAt: { type: string, format: date-time }
        openedAt: { type: [string, 'null'], format: date-time }

    TimeCapsuleDetail:
      allOf:
        - $ref: '#/components/schemas/TimeCapsuleSummary'
        - type: object
          required: [unlockCondition]
          properties:
            unlockCondition: { $ref: '#/components/schemas/UnlockCondition' }
            content:
              type: [string, 'null']
              description: '**`status`가 `OPENED`일 때만 채워집니다.** 그 전에는 `null`'
            media:
              type: array
              description: '**`status`가 `OPENED`일 때만 채워집니다.** 그 전에는 빈 배열'
              items: { $ref: '#/components/schemas/Media' }

    # --- 아카이브 ---

    ArchiveTheme:
      type: string
      enum: [BOOK, POLAROID, ALBUM, SCRAPBOOK]
      description: 책자 · 폴라로이드 · 앨범 · 스크랩북

    CollaborationStatus:
      type: string
      enum: [NONE, OPEN, CLOSED]
      description: '`NONE` 혼자 만드는 중 · `OPEN` 공동 편집 중 · `CLOSED` 종료됨(조회만 가능)'

    CollaboratorRole:
      type: string
      enum: [OWNER, EDITOR]

    CollaboratorStatus:
      type: string
      enum: [INVITED, JOINED, LEFT]

    ArchiveLayout:
      type: object
      description: 테마 캔버스 위의 배치. 좌표와 크기는 캔버스 폭을 1.0으로 본 비율입니다
      required: [x, y, width, height]
      properties:
        x: { type: number, format: double, minimum: 0, maximum: 1 }
        y: { type: number, format: double, minimum: 0, maximum: 1 }
        width: { type: number, format: double, minimum: 0, maximum: 1 }
        height: { type: number, format: double, minimum: 0, maximum: 1 }
        rotation:
          type: number
          format: double
          description: 시계 방향 각도(도). 기본 0

    ArchiveItem:
      type: object
      required: [itemId, media, sortOrder, layout]
      properties:
        itemId:
          type: integer
          format: int64
          description: 아카이브에 배치된 이 한 건의 식별자. 같은 사진을 두 번 배치하면 값이 다릅니다
        media: { $ref: '#/components/schemas/Media' }
        sortOrder: { type: integer, minimum: 1 }
        layout: { $ref: '#/components/schemas/ArchiveLayout' }
        caption:
          type: [string, 'null']
          description: 사진에 붙인 짧은 글(ADR-029)
        isCover: { type: boolean, description: 대표 사진으로 지정된 항목인지 }

    ArchiveCreateRequest:
      type: object
      required: [title, theme]
      properties:
        title: { type: string, minLength: 1, maxLength: 60 }
        theme: { $ref: '#/components/schemas/ArchiveTheme' }
        primaryColor:
          type: [string, 'null']
          pattern: '^#[0-9A-Fa-f]{6}$'
          description: 대표 색상
        tripId:
          type: [integer, 'null']
          format: int64
          description: |
            연결할 여행 일정. 생략 가능합니다(ADR-001).
            **연결해도 아카이브의 여행 기간에는 영향을 주지 않습니다**(ADR-030)

    ArchiveSummary:
      type: object
      required: [archiveId, title, theme, collaborationStatus, itemCount, myRole, createdAt]
      properties:
        archiveId: { type: integer, format: int64 }
        title: { type: string }
        theme: { $ref: '#/components/schemas/ArchiveTheme' }
        primaryColor: { type: [string, 'null'] }
        coverImageUrl:
          type: [string, 'null']
          format: uri
          description: 대표 사진(ARCH-08). 지정하지 않았으면 `null`
        startDate:
          type: [string, 'null']
          format: date
          description: |
            담긴 사진의 촬영 시각 중 가장 이른 날. **서버가 계산하며 입력받지 않습니다**(ADR-030).
            촬영 정보가 있는 사진이 하나도 없으면 `null`
        endDate:
          type: [string, 'null']
          format: date
          description: 담긴 사진의 촬영 시각 중 가장 늦은 날
        itemCount: { type: integer, description: 배치된 사진·영상 수 }
        collaborationStatus: { $ref: '#/components/schemas/CollaborationStatus' }
        myRole:
          $ref: '#/components/schemas/CollaboratorRole'
        linkedTripId:
          type: [integer, 'null']
          format: int64
          description: 연결된 여행 일정. 없으면 `null`
        createdAt: { type: string, format: date-time }

    ArchiveDetail:
      allOf:
        - $ref: '#/components/schemas/ArchiveSummary'
        - type: object
          required: [items, collaborators]
          properties:
            items:
              type: array
              description: '`sortOrder` 오름차순으로 정렬되어 있습니다'
              items: { $ref: '#/components/schemas/ArchiveItem' }
            collaborators:
              type: array
              items: { $ref: '#/components/schemas/Collaborator' }
            originArchiveId:
              type: [integer, 'null']
              format: int64
              description: 복제해서 만든 아카이브면 원본 식별자(ADR-005)

    ArchiveItemsAddRequest:
      type: object
      required: [items]
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 20
          items:
            type: object
            required: [mediaId]
            properties:
              mediaId: { type: integer, format: int64 }
              layout:
                oneOf:
                  - $ref: '#/components/schemas/ArchiveLayout'
                  - type: 'null'
                description: 생략하면 테마의 기본 배치를 서버가 채웁니다
              caption: { type: [string, 'null'], maxLength: 300 }

    ArchiveItemUpdateRequest:
      type: object
      description: 보내지 않은 필드는 변경하지 않습니다
      minProperties: 1
      properties:
        sortOrder: { type: integer, minimum: 1 }
        layout: { $ref: '#/components/schemas/ArchiveLayout' }
        caption: { type: [string, 'null'], maxLength: 300 }

    ArchiveLayoutRequest:
      type: object
      required: [items]
      description: 아카이브의 **모든** 항목을 보내야 합니다. 부분 전송은 거부됩니다
      properties:
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [itemId, sortOrder, layout]
            properties:
              itemId: { type: integer, format: int64 }
              sortOrder: { type: integer, minimum: 1 }
              layout: { $ref: '#/components/schemas/ArchiveLayout' }

    # --- 공동 편집 ---

    Collaborator:
      type: object
      required: [userId, nickname, role, status]
      properties:
        userId: { type: integer, format: int64 }
        nickname: { type: string }
        profileImageUrl: { type: [string, 'null'], format: uri }
        role: { $ref: '#/components/schemas/CollaboratorRole' }
        status: { $ref: '#/components/schemas/CollaboratorStatus' }
        invitedAt: { type: string, format: date-time }
        joinedAt: { type: [string, 'null'], format: date-time }

    CollaborationSession:
      type: object
      description: |
        STOMP over WebSocket 접속 정보(ADR-051).
        메시지 규격은 `docs/realtime-editing.md`에 있습니다
      required: [websocketUrl, topic, activeEditors]
      properties:
        websocketUrl:
          type: string
          format: uri
          description: 접속할 WebSocket 주소. CONNECT 프레임에 액세스 토큰을 실습니다
        topic:
          type: string
          description: '구독 경로. `/topic/archives/{archiveId}` 형태입니다'
        activeEditors:
          type: array
          description: |
            지금 접속해 있는 참여자. 구독을 시작한 뒤에는
            `editor.joined`·`editor.left` 이벤트로 갱신합니다
          items: { $ref: '#/components/schemas/Collaborator' }

    ArchiveUpdateRequest:
      type: object
      description: 보내지 않은 필드는 변경하지 않습니다
      minProperties: 1
      properties:
        title: { type: string, minLength: 1, maxLength: 60 }
        theme: { $ref: '#/components/schemas/ArchiveTheme' }
        primaryColor:
          type: [string, 'null']
          pattern: '^#[0-9A-Fa-f]{6}$'

    # --- 커뮤니티 ---

    PostSortType:
      type: string
      enum: [POPULAR, LATEST]
      default: LATEST
      description: '`POPULAR`은 좋아요 순(ADR-019)'

    ShareType:
      type: string
      enum: [COURSE, ARCHIVE, BOTH]
      description: |
        등록 시점에 무엇을 공유했는지. **원본이 삭제된 뒤에도 남습니다.**
        코스·아카이브 FK가 `ON DELETE SET NULL`이라(ADR-002)
        이 값이 없으면 "처음부터 안 공유함"과 "공유했는데 삭제됨"을 구분할 수 없습니다

    Author:
      type: object
      required: [userId, nickname]
      properties:
        userId: { type: integer, format: int64 }
        nickname: { type: string }
        profileImageUrl: { type: [string, 'null'], format: uri }

    PostSummary:
      type: object
      required: [postId, author, content, likeCount, commentCount, regions, likedByMe, createdAt]
      properties:
        postId: { type: integer, format: int64 }
        author: { $ref: '#/components/schemas/Author' }
        content: { type: string, description: 피드에서는 앞부분만 잘라 보낼 수 있습니다 }
        thumbnailUrl:
          type: [string, 'null']
          format: uri
          description: 공유한 아카이브의 대표 사진, 없으면 코스 첫 장소의 이미지
        likeCount: { type: integer }
        commentCount: { type: integer, description: 대댓글을 포함한 수 }
        likedByMe: { type: boolean }
        regions:
          type: array
          description: 등록 시점에 굳힌 지역 스냅샷(ADR-003)
          items: { $ref: '#/components/schemas/RegionSummary' }
        createdAt: { type: string, format: date-time }

    PostDetail:
      allOf:
        - $ref: '#/components/schemas/PostSummary'
        - type: object
          required: [shareType]
          properties:
            shareType: { $ref: '#/components/schemas/ShareType' }
            sharedCourse:
              oneOf:
                - $ref: '#/components/schemas/TripSummary'
                - type: 'null'
              description: 공유한 코스. 삭제되었으면 `null`
            sharedArchive:
              oneOf:
                - $ref: '#/components/schemas/ArchiveSummary'
                - type: 'null'
              description: 공유한 아카이브. 삭제되었으면 `null`
            deletedShare:
              type: array
              description: |
                `shareType`에는 있는데 원본이 사라진 것들(COMM-17).
                앱은 이 값으로 "삭제된 기록입니다"를 표시합니다
              items: { type: string, enum: [COURSE, ARCHIVE] }
            updatedAt: { type: [string, 'null'], format: date-time }

    PostCreateRequest:
      type: object
      required: [content]
      description: '`tripId`와 `archiveId` 중 최소 하나는 있어야 합니다'
      properties:
        content: { type: string, minLength: 1, maxLength: 2000 }
        tripId: { type: [integer, 'null'], format: int64 }
        archiveId: { type: [integer, 'null'], format: int64 }
        regionCodes:
          type: array
          description: |
            코스를 공유하지 않을 때 **필수**입니다(COMM-04).
            코스를 공유하면 보내지 않아도 서버가 일정에서 자동으로 뽑습니다
          items: { type: string }

    LikeResult:
      type: object
      required: [postId, likeCount, likedByMe]
      properties:
        postId: { type: integer, format: int64 }
        likeCount: { type: integer }
        likedByMe: { type: boolean }

    # --- 댓글 ---

    CommentStatus:
      type: string
      enum: [ACTIVE, DELETED]

    CommentWriteRequest:
      type: object
      required: [content]
      properties:
        content: { type: string, minLength: 1, maxLength: 500 }

    Reply:
      type: object
      required: [replyId, parentCommentId, author, status, createdAt]
      properties:
        replyId: { type: integer, format: int64 }
        parentCommentId: { type: integer, format: int64 }
        author: { $ref: '#/components/schemas/Author' }
        content:
          type: [string, 'null']
          description: 삭제된 대댓글은 `null`
        status: { $ref: '#/components/schemas/CommentStatus' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: [string, 'null'], format: date-time }

    Comment:
      type: object
      required: [commentId, author, status, replyCount, createdAt]
      properties:
        commentId: { type: integer, format: int64 }
        author: { $ref: '#/components/schemas/Author' }
        content:
          type: [string, 'null']
          description: |
            삭제된 댓글은 `null`입니다. 대댓글이 달려 있으면 자리는 남으므로(ADR-007)
            앱은 "삭제된 댓글입니다"로 표시합니다
        status: { $ref: '#/components/schemas/CommentStatus' }
        replyCount: { type: integer }
        replies:
          type: array
          description: 앞의 몇 개만 미리 담깁니다. 나머지는 `GET /comments/{commentId}/replies`
          items: { $ref: '#/components/schemas/Reply' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: [string, 'null'], format: date-time }

    # --- 신고 ---

    ReportReason:
      type: string
      enum: [OBSCENE, ABUSE, SPAM, FALSE_INFO, ETC]
      description: |
        `OBSCENE` 음란물 · `ABUSE` 욕설·혐오 · `SPAM` 광고·도배 ·
        `FALSE_INFO` 허위 정보 · `ETC` 기타(ADR-046)

    ReportRequest:
      type: object
      required: [reason]
      properties:
        reason: { $ref: '#/components/schemas/ReportReason' }
        detail:
          type: [string, 'null']
          maxLength: 200
          description: |
            덧붙일 설명. `reason`이 `ETC`이면 필수입니다 —
            "기타"만으로는 관리자가 판정할 수 없습니다

    ReportResult:
      type: object
      required: [reportId, status]
      properties:
        reportId: { type: integer, format: int64 }
        status:
          type: string
          enum: [PENDING]
          description: 접수 시점에는 항상 `PENDING`입니다. 판정은 관리자가 합니다(ADMIN-09)

    # --- 공개 프로필 ---

    PublicProfile:
      type: object
      description: 다른 사용자에게 보이는 정보. 내 프로필(`MyProfile`)보다 좁습니다
      required: [userId, nickname, postCount]
      properties:
        userId: { type: integer, format: int64 }
        nickname: { type: string }
        profileImageUrl: { type: [string, 'null'], format: uri }
        postCount: { type: integer, description: 공개된 게시물 수 }

    # --- 알림 ---

    NotificationType:
      type: string
      enum: [COLLAB_INVITE, CAPSULE_UNLOCK, WARNING]
      description: |
        `COLLAB_INVITE` 공동 편집 초대(NOTI-02) · `CAPSULE_UNLOCK` 타임캡슐 해제(TC-04) ·
        `WARNING` 경고(NOTI-05). **차단은 알림으로 오지 않습니다**(AUTH-06)

    Notification:
      type: object
      required: [notificationId, type, title, isRead, createdAt]
      properties:
        notificationId: { type: integer, format: int64 }
        type: { $ref: '#/components/schemas/NotificationType' }
        title: { type: string }
        body: { type: [string, 'null'] }
        targetType:
          type: [string, 'null']
          enum: [ARCHIVE, TIME_CAPSULE, SANCTION, 'null']
          description: 눌렀을 때 이동할 대상의 종류
        targetId: { type: [integer, 'null'], format: int64 }
        isRead: { type: boolean }
        createdAt: { type: string, format: date-time }

    NotificationSetting:
      type: object
      required: [type, enabled, editable]
      properties:
        type: { $ref: '#/components/schemas/NotificationType' }
        enabled: { type: boolean }
        editable:
          type: boolean
          description: '`false`면 사용자가 끌 수 없습니다. 경고 알림이 여기 해당합니다'

    # --- 관리자 ---

    UserStatus:
      type: string
      enum: [ACTIVE, WARNED, BLOCKED, WITHDRAWN]

    SanctionType:
      type: string
      enum: [WARNING, BLOCK, UNBLOCK]

    SanctionSource:
      type: string
      enum: [MANUAL, AUTO]
      description: '`AUTO`는 누적 유효 신고로 시스템이 건 제재(ADMIN-10)'

    SanctionRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 1, maxLength: 200 }

    Sanction:
      type: object
      required: [sanctionId, userId, type, source, reason, createdAt]
      properties:
        sanctionId: { type: integer, format: int64 }
        userId: { type: integer, format: int64 }
        type: { $ref: '#/components/schemas/SanctionType' }
        source: { $ref: '#/components/schemas/SanctionSource' }
        reason: { type: string }
        issuedByAdminName:
          type: [string, 'null']
          description: 자동 제재면 `null`
        createdAt: { type: string, format: date-time }

    AdminSession:
      type: object
      required: [accessToken, adminName]
      properties:
        accessToken: { type: string }
        refreshToken: { type: [string, 'null'] }
        adminName: { type: string }

    AdminDashboard:
      type: object
      description: 운영 지표와 콘텐츠 지표 두 갈래입니다(ADR-035)
      required: [totalUserCount, newUserCountToday, pendingReportCount, sanctionedUserCount, totalArchiveCount, totalTripCount, totalPostCount]
      properties:
        totalUserCount:
          type: integer
          description: '**운영** — 전체 회원 수. 탈퇴한 회원은 제외'
        newUserCountToday:
          type: integer
          description: '**운영** — 오늘 가입한 회원 수'
        pendingReportCount:
          type: integer
          description: '**운영** — 아직 판정하지 않은 신고 수. 0보다 크면 처리할 일이 있습니다'
        sanctionedUserCount:
          type: integer
          description: '**운영** — 경고 또는 차단 상태인 회원 수'
        totalArchiveCount:
          type: integer
          description: '**콘텐츠** — 만들어진 아카이브 수'
        totalTripCount:
          type: integer
          description: '**콘텐츠** — 만들어진 여행 일정 수'
        totalPostCount:
          type: integer
          description: '**콘텐츠** — 커뮤니티에 올라온 게시물 수. 삭제된 것은 제외'

    AdminUserSummary:
      type: object
      required: [userId, nickname, status, validReportCount, createdAt]
      properties:
        userId: { type: integer, format: int64 }
        nickname: { type: string }
        status: { $ref: '#/components/schemas/UserStatus' }
        validReportCount:
          type: integer
          description: 유효로 판정된 누적 신고 횟수. 1이면 경고, 3이면 차단(ADMIN-10)
        createdAt: { type: string, format: date-time }

    AdminUserDetail:
      allOf:
        - $ref: '#/components/schemas/AdminUserSummary'
        - type: object
          required: [sanctions]
          properties:
            socialProviders:
              type: array
              description: 연결된 소셜 계정 제공자
              items: { type: string, enum: [google, kakao, naver] }
            postCount: { type: integer }
            commentCount: { type: integer }
            sanctions:
              type: array
              description: 경고·차단·해제 이력. 최근 것이 앞에 옵니다
              items: { $ref: '#/components/schemas/Sanction' }
            withdrawnAt: { type: [string, 'null'], format: date-time }

    ReportStatus:
      type: string
      enum: [PENDING, VALID, REJECTED]

    AdminReportSummary:
      type: object
      required: [reportId, targetType, targetId, reason, status, reporter, createdAt]
      properties:
        reportId: { type: integer, format: int64 }
        targetType: { type: string, enum: [POST, COMMENT] }
        targetId: { type: integer, format: int64 }
        targetAuthor: { $ref: '#/components/schemas/Author' }
        reason: { $ref: '#/components/schemas/ReportReason' }
        detail: { type: [string, 'null'], description: 신고자가 덧붙인 설명 }
        status: { $ref: '#/components/schemas/ReportStatus' }
        reporter: { $ref: '#/components/schemas/Author' }
        createdAt: { type: string, format: date-time }

    AdminReportDetail:
      allOf:
        - $ref: '#/components/schemas/AdminReportSummary'
        - type: object
          properties:
            targetContent:
              type: [string, 'null']
              description: 신고당한 게시물·댓글의 실제 내용. 이미 삭제되었으면 `null`
            targetDeleted:
              type: boolean
              description: 신고 대상이 이미 삭제된 상태인지
            reviewedByAdminName: { type: [string, 'null'] }
            reviewedAt: { type: [string, 'null'], format: date-time }
            note: { type: [string, 'null'] }

    ReportReviewResult:
      type: object
      required: [report, targetUserValidReportCount]
      properties:
        report: { $ref: '#/components/schemas/AdminReportDetail' }
        targetUserValidReportCount:
          type: integer
          description: 판정 후 신고당한 회원의 누적 유효 신고 횟수
        appliedSanction:
          oneOf:
            - $ref: '#/components/schemas/Sanction'
            - type: 'null'
          description: |
            누적 횟수가 기준에 닿아 시스템이 자동으로 건 제재(ADMIN-10).
            기준에 닿지 않았거나 반려 판정이면 `null`
